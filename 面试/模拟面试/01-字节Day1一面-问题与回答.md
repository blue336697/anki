# 面试导出 01：字节 Day1 一面

- 导出时间：2026-05-15 00:26:03
- 来源：Hermes 历史会话 / 微信交互记录
- 范围：2026-04-20~2026-04-21，字节风格 Day1 一面；包含自我介绍、发票/流水解析规则平台、性能口径、风险边界、AI 协同开发等问答。

说明：以下按历史消息顺序导出。
- “面试官”包含正式问题、追问、纠偏和阶段复盘。
- “候选人”是你的原始回答。
- 已过滤系统重置提示、空回复、记忆工具失败提示等非面试内容。

## Day1 一面

### 1. 候选人回答

用字节风格，开始 day1 一面

### 2. 面试官问题 / 追问 / 反馈

好，直接开始。

**模式**：分轮次循环  
**公司风格**：字节  
**当前轮次**：**Day 1 一面**  
**要求**：我会按真实面试来，偏高压，会打断，会追问。你回答别空，别绕，别讲团队光环。

先提醒你三条：
1. **先说结论，再补细节**
2. 项目题默认按 **STAR** 或 **背景→目标→动作→结果→反思**
3. 只要你说“我负责/我优化了/提升了 xx%”，我一定追问证据

---

### 第 1 题：自我介绍
**1 分钟以内**，按这个结构答：

- 你现在是谁，做什么方向
- 过去几段经历主线是什么
- 你最能打的 2 个项目是什么
- 为什么你现在能投 **字节偏业务/全栈/AI 协同开发** 这类岗位

**限制：**
- 不要从大学开始背
- 不要泛泛说“做过很多项目”
- 不要讲成流水账
- 要让我在 1 分钟内听出来：**你的定位、强项、业务价值**

开始答。

### 3. 面试官问题 / 追问 / 反馈

(empty)

### 4. 候选人回答

You just executed tool calls but returned an empty response. Please process the tool results above and continue with the task.

### 5. 面试官问题 / 追问 / 反馈

刚才那次空回复是我的问题。  
我实际做了两件事：

- 尝试保存一条记忆：**你会用字节风格做 Day 1 一面模拟**
- 结果：**memory 工具当前不可用**，所以没保存成功
- 现在我已经重新加载并确认了你刚创建的 skill：`interview-coach-fullstack-ai`

那就按这个 skill 继续，直接进入：

**模式**：分轮次循环  
**公司风格**：字节  
**当前轮次**：**Day 1 一面**

规则不变：
1. 先说结论，再补细节  
2. 别讲团队光环，讲你自己  
3. 你说“我负责 / 我优化 / 提升 xx%”，我会追问证据

---

### 第 1 题：自我介绍
**1 分钟以内**，按这个结构答：

- 你现在是谁，做什么方向
- 过去几段经历主线是什么
- 你最能打的 2 个项目是什么
- 为什么你现在能投 **字节偏业务 / 全栈 / AI 协同开发** 这类岗位

**限制：**
- 不要从大学开始背
- 不要流水账
- 不要空泛地说“做过很多项目”
- 要让我在 1 分钟内听出来：**你的定位、强项、业务价值**

开始答。

### 6. 候选人回答

嗯，你好，面试官，我现在就职于百荣云创公司，主要负责的业务就是智能风控业务。现在正在做的就是银行流水解析和发票解析。主要现在负责其中的政一个政策平台的搭建，主要解决就是从主要解决就是响应快的问题啊，响应慢的问题。因为如果通过OCR或者是通过文本解析的话，嗯，是非常慢的。然后，现在主要是拿到这个文本，就通过正则去匹匹这个匹这个关键字，然后将一些比较简单的简单的一些发票或者流水通过配置的方式，通过配置政则的方式就能快速拿到结果。主要现在就在做这个业务，嗯，我目前的话为什么投这个，是因为我想要追随时代发展，ai 的发展，也想要学习最前沿的 ai

### 7. 面试官问题 / 追问 / 反馈

好，先打断你。

你这段自我介绍有几个明显问题：

1. **重复太多**，像“主要解决”“通过配置”“匹这个关键字”反复出现，不像准备好的面试表达。  
2. **信息密度不够**，我没听清你的平台到底是什么、你承担的职责边界是什么、结果指标是什么。  
3. **动机太空**，“追随 AI 发展”这种话谁都会说，不构成有效说服力。  
4. **缺少结果导向**，你讲了做法，但没讲清楚：
   - 响应到底从多少降到多少？
   - 覆盖了哪些票据/流水类型？
   - 配置化之后，业务收益是什么？

现在按 **1 分钟、STAR 结构** 重答，不要铺垫，不要口语化重复。

你必须覆盖这 5 个点：

- 你现在公司和岗位
- 核心业务场景是什么
- 你负责的具体模块是什么
- 你做了什么关键动作
- 最终结果和量化指标是什么

我给你一个更接近合格的结构模板，你按这个说，但内容必须换成你自己的真实经历：

> 我目前在 xx 公司负责智能风控相关业务开发，主要场景是银行流水解析和发票解析。  
> 在这个方向上，我重点负责 xx 政策/规则平台的搭建与优化。  
> 当时的核心问题是传统 OCR 或全文解析链路响应较慢，无法满足部分简单票据和流水的实时处理需求。  
> 我主导/负责了一个配置化解析平台，把部分标准化样本的识别逻辑从重模型/重解析链路前置为规则匹配链路，通过关键词提取、正则模板配置和可扩展规则管理，实现了简单场景的快速解析。  
> 最终带来的结果是 xx 指标提升 / xx 响应时延下降 / xx 覆盖率提升 / xx 人工成本下降。  
> 我现在想看更大的平台和更前沿的 AI 应用落地场景，所以来面这个岗位。

现在，**你重答一遍**。  
要求：**控制在 1 分钟，先讲结论，别讲虚话。**

### 8. 候选人回答

你好你好你好

### 9. 面试官问题 / 追问 / 反馈

停。

这不是回答，这是在回避问题。真实面试里我会直接记你一次**表达失控**。

我刚才的问题很明确：  
**请你用 1 分钟重新做自我介绍。**

要求再重复一遍，只保留这 5 个点：

1. 你现在公司和岗位  
2. 业务场景  
3. 你负责的模块  
4. 你做的关键动作  
5. 量化结果

你可以直接按这个句式说：

> 我目前在 XX 公司担任 XX，主要负责 XX 业务。  
> 当前核心场景是 XX。  
> 我主要负责 XX 平台/模块。  
> 为了解决 XX 问题，我做了 XX。  
> 最终把 XX 从 XX 提升到 XX，或者把 XX 时延从 XX 降到 XX。

**现在开始，1 分钟内，说完整。**

### 10. 候选人回答

我目前在百荣云创担任研发工程师，主要负责的项目是智能风控，主要负责的业务是其中银行流水解析和发票流水解析。对此目前正在负责的是一个目前正在做的是一个平台，正则平台是让可以通过配置化的方式快速地读取文本中关键的信息，然后使其从原本一份解析、一份流水或者一份发票，从原先的10秒到1分钟缩短至毫秒级

### 11. 面试官问题 / 追问 / 反馈

这版比刚才强一些，至少有主线了，但仍然**不合格**。问题我直接说：

### 你这版的主要问题
1. **表达仍然重复**
   - “目前正在负责的是一个目前正在做的是一个平台”
   - 这种口误在真实面试里会直接拉低印象分。

2. **概念不准确**
   - 你前面说的是“银行流水解析和发票解析”，后面又说“发票流水解析”，概念混了。
   - 面试官会怀疑你对自己业务都没讲清。

3. **结果表述有风险**
   - “从原先的10秒到1分钟缩短至毫秒级”
   - 这个区间太大，而且“毫秒级”很容易被追着打：
     - 是 p50 还是 p95？
     - 端到端链路还是规则命中链路？
     - 包不包含 OCR？
   - 如果你答不上来，就会被判断成**夸大指标**。

4. **缺少你的动作**
   - 我听到了平台和结果，但没听清楚：
     - 你是做了规则引擎？
     - 还是做了配置平台？
     - 还是做了匹配链路前置？
     - 还是做了模板抽象和运营配置？
   - 面试官要的是 **“你具体做了什么”**。

---

### 我给你压缩成一个更像样的版本
你可以参考这种表达：

> 我目前在百融云创担任研发工程师，主要负责智能风控相关业务开发，核心场景是银行流水解析和发票解析。  
> 我当前重点负责一个配置化规则平台建设，目标是针对结构相对稳定的流水和发票场景，跳过较重的 OCR/全文解析链路，通过关键词提取和正则规则配置，快速抽取关键信息。  
> 我在其中主要负责规则平台的设计和落地，包括规则配置、匹配逻辑和解析流程优化。  
> 这个平台上线后，把部分简单样本的解析耗时从原来的秒级降低到了接近实时处理，显著提升了响应速度和业务处理效率。

注意，我这里故意没替你写死“毫秒级”“10 秒到 1 分钟”，因为**你现在这个指标说法不够稳**。

---

现在开始追问，不给你喘气。

## 问题 1：项目深挖
你刚才说你在做“正则平台”或者“配置化规则平台”。

**你用 STAR 结构，1 分钟回答：**
1. 这个平台出现之前，原来的解析链路长什么样？
2. 为什么会慢？慢在 OCR、文本预处理、规则提取，还是模型推理？
3. 你这个平台到底插在整条链路的哪个位置？
4. 哪些单据走规则链路，哪些还必须走原来的重链路？
5. 你个人负责的部分到底是什么？

**别再讲团队，讲你自己。**  
**先说结论，再补细节。**

### 12. 候选人回答

嗯，这个平台出现之前呢，整个链路是接收到发票，然后调用第三方的一个发票识别平台，一个合合的平台里边儿，他会给我们提供他处理好的KV，以及这个KV所出的一个整个发票的一个位置信息。你这就是原来链路，然后，我做这个平台就是为了替换这个第三方平台。然后，我是相当于把我们公司的OCR和文本识别分装了一层，然后进行了一个正则提取，这其中会有一些降级策略，当正则或和那个。哦，当正则提取出来，比如说它的核心字段不足百分之多少，或者说是它的那个总字段不足百分之二一些的降级策略，然后会走最终的，调用第三方就也就会走到原来的链路上面我个人负责就是整个系统的前后端开发，因为现在是web coding. 这两个前段后端都是我在开发，也是我在运维的

### 13. 面试官问题 / 追问 / 反馈

停，我先给你面评式反馈：

这版比上一版**信息更多**，但还是有几个致命问题：

### 你这版的问题
1. **结构乱**
   - 你没有按我要求的 5 个点答，想到哪说到哪。
   - 高压面里，这会被判断为**结构化表达弱**。

2. **术语不准**
   - “合合平台”
   - “KV 所出的一个整个发票的位置信息”
   - “文本识别分装了一层”
   这些都不专业。你要说：
   - 第三方 OCR/KV 服务
   - 字段级 KV 结果与位置信息
   - 对公司 OCR 能力做了一层封装与规则解析编排

3. **ownership 还不够硬**
   - 你说“前后端都是我开发，也我运维”，这是加分点。
   - 但你没说清楚你到底**设计了哪些核心能力**：
     - 规则配置模型？
     - 命中策略？
     - 降级策略？
     - 字段完整性校验？
     - 页面配置和发布流程？
   - 这些才是能证明你是 owner 的东西。

4. **业务边界还没讲透**
   - 不是所有发票都能走规则链路。
   - 面试官下一步一定追问：**什么样的样本适合规则化，什么样的不适合？**

---

### 如果是更好的回答，应该像这样
你可以学这个表达方式：

> 在这个平台上线之前，发票解析链路主要依赖第三方 OCR/KV 服务。系统接收到发票后，会先调用第三方平台识别，返回结构化字段结果和位置信息，再进入我们后续处理流程。  
> 这条链路的问题是响应时间比较长，而且对一些格式相对固定、字段规则明确的发票场景来说，直接走完整识别链路成本偏高。  
> 所以我做了一个配置化规则平台，在公司已有 OCR 和文本识别结果之上，增加了一层规则解析能力，通过关键词和正则模板直接抽取核心字段。  
> 同时我设计了降级策略：如果核心字段缺失率或整体字段完整度不达标，就自动回退到原来的第三方链路，保证准确率。  
> 我个人负责这个系统的前后端开发和整体落地，包括配置平台、规则匹配逻辑、降级策略以及上线后的运维。

这就比你刚才那种口语化表述强很多。

---

现在开始真正深挖。  
**别泛泛讲，按点回答。**

## 问题 2：技术深挖
你说你做了“降级策略”。

我现在追问你 6 个点，你必须尽量具体：

1. **核心字段不足百分之多少会降级？这个阈值是谁定的，怎么定的？**  
2. **字段完整度怎么计算？**  
   - 是按字段数？
   - 按字段权重？
   - 还是按业务必填项？
3. **为什么不能只要正则命中就直接返回？为什么还要回退第三方？**
4. **如果正则错提了字段，但字段完整度看起来很高，会不会产生误判？你怎么控制？**
5. **你这个平台的规则是怎么组织的？**
   - 按票据类型？
   - 按模板版本？
   - 按字段级规则？
6. **如果新增一种发票模板，接入流程是什么？要不要改代码？**

### 回答要求
- 先说“降级策略的设计原则”
- 再回答这 6 点
- 控制在 **2 分钟内**
- 不要说“差不多”“大概”“应该”，能量化就量化，量化不了就说你当时是怎么判断的

**现在答。**

### 14. 候选人回答

字段阈值现在是80%会出，核心的依据的话，就是我觉得，如果你字段，假如说有10个字段的话，你连其中的8个字段都解析不出来，那么我觉得你这个你这个结果也是不理想的
嗯，字段的完整度是由他去政策平台上配置这个字段的解析规则，比如说，然后一个发票有很多很多字，但是他只关注其中的某几个，所以它只配了某几个字段的正则解析规则，那么它配置的这个数量就是它代表它这个字段的完整度
嗯，我是说他当他不满足于我设置的这个降级的阈值，或者说是字段完整度也不满足，他就会再调用第三方接口，直接拿到这个人家现成解析好的K，我这边就不需要再解析了，这是一种降级策略
如果正则提取到了错的字段，并且字段完整度还很高的话，现在对于这种情场景下是没有处理的
嗯，当前这个政策平台是基于我的上游返回过来的返回给我的文档类型。当我输入一个媒体的时候，我问这个三方接口会返回给我这个文档是属于什么是发票呀，还是病例或者说是什么银行流水。然后我根据这个类型去绑定所关联的那些正则
嗯，这个平台的目标就是零上线的一个目标。就是说，当你一个来了一个新的模板，我们不认识的，就可以通过一定的规则，就是正则规则，去提取里边儿的KV，当然如果说第三方那个返回给我的文档类型是不准确的，或者他返回给我的是未知的，那么这个就会有一些问题。我当年的一个补救措施就是我这边是允许。如果识别到一个未知的话，我我会单独进行处理，然后给这些政则关联上一个新的类型

### 15. 面试官问题 / 追问 / 反馈

停。

这段回答里，**有亮点，但也暴露了一个很危险的问题：你现在的方案是能跑，但不够严谨。**  
如果这是二面/三面，面试官已经会开始怀疑你：

- 规则平台是否真的可控
- 阈值是不是拍脑袋
- 错提字段怎么兜底
- “零上线”是不是只是理想，不是工程现实

我逐条拆你这次回答。

## 先说优点
1. 你把平台目标讲出来了：**配置化、少改代码、尽量零上线**
2. 你讲到了几个关键机制：
   - 阈值降级
   - 按文档类型绑定规则
   - 未知类型兜底
3. 你没有硬装懂，直接承认了：
   - **错提字段但完整度高，目前没处理**
   
这句虽然扣分，但比胡编强。

---

## 再说致命问题

### 1. 阈值“80%”明显像拍脑袋
你原话大意是：

> 如果 10 个字段里 8 个都提不出来，那结果不理想。

这不是工程答案，这是直觉答案。  
面试官会继续追：

- 为什么不是 70%？
- 为什么不是按字段权重？
- 发票字段的重要性一样吗？
- 发票号码错了和备注缺失，风险一样吗？

更好的答法应该是：

> 初期阈值是经验值，后续应该基于样本回放和业务必填字段来校准。  
> 实际上不能简单按字段数量判断，而要区分核心字段和普通字段。  
> 比如金额、税额、票号、日期这类关键字段的权重应该更高，只要关键字段缺失或校验失败，就应优先降级。

你现在的问题不是不会做，是**不会把“经验决策”包装成“可迭代的工程决策”**。

---

### 2. 你把“字段完整度”和“配置字段数量”混在一起了
你刚才其实表达的是：

> 配了几个字段，就拿这几个字段来算完整度。

这不够严谨。  
完整度至少要区分三层：

1. **规则覆盖度**
   - 这个模板一共配置了多少字段规则

2. **字段命中度**
   - 实际命中了多少字段

3. **关键字段通过率**
   - 核心字段是否命中且校验通过

如果你只说“配了几个字段就代表完整度”，面试官会觉得你在混淆概念。

---

### 3. 你自己承认了一个很大的漏洞
你说：

> 如果正则提取到了错的字段，并且字段完整度很高，目前没有处理。

这在真实面试里是**高危问题**。  
因为这意味着：

- 你的系统可能返回一个“看起来完整、实际上错误”的结果
- 这比“返回空”更危险
- 在风控业务里，**错判通常比慢更危险**

你必须学会补一句：

> 这是我当时方案的一个明显边界。  
> 如果继续演进，我会增加字段级校验，例如金额格式校验、日期合法性校验、票号规则校验，以及字段间一致性校验，避免“高完整度但低正确率”的误判。

这句话一补，立刻从“漏洞暴露”变成“有复盘能力”。

---

### 4. “零上线”这个说法风险很大
你说平台目标是零上线。  
这句话本身没问题，但你不能说得像绝对承诺。

更稳的表达是：

> 平台目标是尽量减少因新增模板导致的代码发布，通过配置新增或调整规则完成大部分适配；但如果上游文档类型识别不准，或者出现全新结构模板，仍然可能需要补充类型映射、校验规则甚至代码层兜底。

这样才像做过工程的人。

---

## 这题如果是合格回答，应该这么说
你可以参考这版：

> 我的降级策略设计原则是：优先保证结果可用性和准确性，不能为了追求快而输出错误结果。  
> 阈值初期我们用了 80% 作为经验阈值，但实际上不是简单按字段数量判断，而是结合核心字段是否命中来决定是否降级。像金额、日期、票号这类关键字段，如果缺失或者解析失败，即使整体字段命中率高，也应该回退第三方链路。  
> 字段完整度的计算基础是当前模板配置了哪些目标字段，再看这些字段中实际成功提取了多少；后续更合理的做法应该是加入字段权重，而不是纯数量统计。  
> 之所以不能正则命中就直接返回，是因为正则可能误提，尤其在 OCR 文本存在噪声时，会出现“字段看起来齐全但内容不正确”的情况，所以必须结合阈值、关键字段和必要的字段校验一起判断。  
> 当前平台规则主要是按上游识别出的文档类型进行组织，每种类型关联一组字段规则。新增模板时，目标是尽量通过新增或调整配置完成接入，不改代码；但如果文档类型识别异常或模板结构完全新颖，就需要增加新的类型映射和兜底处理。

---

现在我继续压你，进入**真实性 + ownership + 架构能力**追问。

## 问题 3：你说前后端都是你做，还是你运维
那我现在不听空话，我要听系统设计。

你回答这 5 个点，按“模块 -> 作用 -> 你做的决定”来讲：

1. **前端页面你做了哪些核心功能？**
   - 规则配置？
   - 测试预览？
   - 发布上线？
   - 命中结果对比？
   - 版本管理？

2. **后端系统你拆成了哪些核心模块？**
   - 规则管理
   - 解析执行
   - 文档类型路由
   - 降级调用
   - 日志审计
   - 配置发布
   你到底怎么拆的？

3. **规则是怎么存的？**
   - 数据库表结构大概怎么设计？
   - 一条规则存哪些信息？
   - 怎么支持一个类型对应多个字段？

4. **一次解析请求进来后的执行流程是什么？**
   - 从接收图片/文本
   - 到拿 OCR 结果
   - 到命中规则
   - 到校验
   - 到降级
   - 到最终返回

5. **你上线后怎么看平台效果？**
   - 你监控哪些指标？
   - 命中率？
   - 降级率？
   - 正确率？
   - 平均耗时？
   - 第三方调用节省量？

### 回答要求
- 不要泛泛而谈
- 你不会的地方可以直接说“当时没做到”
- 但你必须把**你真实做过的部分说扎实**

**现在答。**

### 16. 候选人回答

嗯，前端的话，我目前就是把它当做一个运维平台来设计的，所以它主要分主要就有四个管理页面，一个是KV结构的正则配置页面，一个是table的配置，table是什么意思呢？就是说，当你一个表格，当你一个文件传上来，它可能是个银行流水。银行流水的话，它下面儿可能都是一些非常规则的一些list，它是一条目的嘛，所以它可以被转换成list，这是一个配置，这是部分配置是通过HTML配的。因为调用OCR的结果，它会对于这种表格类的，他会给我用一个table的标签，给我分装。然后还有一个，就是我前面说的那个文档的一个新增或者修改，文档类别的新增和修改啊，就这三部分。就是这部分是配置的，还有一部分是日志的，就是我会观测他日志，然后最后是一个效果，就是我会做一个弹幕效果。这个接口肯定是暴露给直接通过，要么通过分，要么通过HTP调用。但是我在页面，然后也对接了这个。传入媒体，然后直接生成结果，这样方便我调试
后端的话，我主要就是设计了文档类型的映射降级策略，然后还有日志管理，然后它的体以及。内似的各种解析策略，还有一些数据标准的一些数据清洗，比如说去除或者替换单元格里边儿的空格是这样
嗯，我目前的规则是这样设计的，是每一数据库的每一条目是关联一个类别下的一个字段，这个字段可能会有多条的正则的提取，然后是这样设计的，规则主要存它是否启用，以及它正则匹配。我还除了正则，还支持一些关键字的匹配，或者说是包含关系的。匹配，所以这个每一条规则是有三种类别，一种是正则，一种是关键字，还有一种是包含，
对一个柜，一种类别是支持多种规则的
嗯，一个正常的一个流程是，嗯，这个服务拿到媒体文件以后先去调用文档类型识别。如果文档类型识别的话，我这边会做一个默认的未知处理，然后直接return。然后，像如果说是拿到了相关类别，就去先去调用那个并发的，去调用我的三方接口，就是OCR和文本识别这两个接口。然后，两者同时拿到结果，会优先以OCR的结果为准去提取信息。接下来，拿到这两种结果以后，就去数据库加载对应的。TV正则规则和list的正则规则，list的提取规则这不拿出来以后，然后就开始提取。提取完以后，就以刚刚我前面说到的一些降级策略，然后来判断它是否最终会走第三方的兜底策略。兜底就是整个文件都发给三方接口，直接返回我想要的那种KV结构，这是一个兜底策略，但是这个结果肯定是不快的
嗯，我日志现在就记录了它的耗时以及它的那个解析出来的配置的字段数量以及解析后的字段数量，就就现在只监控了这三个指标，后续会考虑继续往再加一些

### 17. 面试官问题 / 追问 / 反馈

好，这一轮终于开始像样了。  
你现在**至少把系统轮廓讲出来了**，这比前面强很多。

但如果我是二面面试官，我会给你一个判断：

> **你确实做过这个平台，而且参与度不低。**  
> 但是你的表达还停留在“我知道系统怎么跑”，还没到“我能把系统设计讲成一个成熟工程方案”的层级。

我分开说。

---

## 一、这次回答里的加分点

### 1. 你讲出了前端不是普通页面，而是“运维/配置平台”
这点是加分的。因为它说明你不是只写了个接口，而是在做一个**可运营、可配置、可调试**的平台。

你提到的几个功能是对的：

- KV 结构规则配置
- table/list 类规则配置
- 文档类别新增/修改
- 日志观测
- 页面内上传媒体直接调试结果

这说明你有一点**平台化意识**，不是只做算法调用。

---

### 2. 你把后端核心职责讲出来了
至少我现在听出来你后端做了这些事：

- 文档类型识别后的路由
- OCR / 文本识别并发调用
- 规则加载
- 规则提取
- 降级兜底
- 数据清洗
- 日志记录

这已经具备一个基础的**执行链路设计**了。

---

### 3. 你把规则模型讲出了一点雏形
虽然还不够专业，但我能听出来你在做的是：

- 一个文档类型下
- 有多个目标字段
- 每个字段可以挂多条规则
- 规则类型包括：
  - 正则
  - 关键字
  - 包含匹配
- 每条规则有启用状态

这个方向是对的。至少说明你不是把规则硬编码在代码里。

---

### 4. 你说出了真实监控指标
你没乱编一堆高级指标，而是老实说：

- 耗时
- 配置字段数量
- 解析后的字段数量

虽然不够，但这至少像真实项目，不像背答案。

---

## 二、这次回答的主要扣分点

### 1. 表达太散，术语不稳
你现在最大的问题仍然不是“没做”，而是**说不稳**。

比如：

- “这部分是通过 HTML 配的”
- “table 标签给我封装”
- “体以及内似的各种解析策略”
- “直接 return”

这种表达很像平时聊天，不像面试。  
你要把它转成工程语言：

- 表格类文档支持基于 OCR 返回的 table 结构进行 list 抽取
- 后端按文档类型加载字段级规则与表格级规则
- 解析链路支持未知类型短路返回和第三方兜底回退
- 平台内置调试入口，支持媒体上传与结果预览

这就是差距。

---

### 2. 规则设计还不够完整
你现在讲的是“规则能配”，但没讲出**规则治理能力**。

面试官会继续怀疑：

- 规则有没有优先级？
- 多条规则同时命中怎么办？
- 不同规则冲突怎么办？
- 发布后能不能回滚？
- 配置变更有没有版本号？
- 测试数据和生产规则怎么隔离？

如果这些都没有，你的平台更像“可配置脚本集合”，还不是成熟平台。

---

### 3. unknown 类型直接 return，这个设计有风险
你刚才说：

> 如果文档类型识别是未知，我这边会做默认未知处理，然后直接 return。

这里风险很大。  
为什么？

因为如果上游类型识别本身不稳定，那你这里就会：

- 直接丢失可解析机会
- 或者导致业务失败
- 而不是继续走重链路兜底

更合理的设计通常是：

> unknown 类型不要直接终止，应根据业务要求选择：
> 1. 直接走第三方全量解析兜底  
> 2. 进入人工标注/补录队列  
> 3. 记录样本用于新增规则

你现在这个点，很容易被抓住打。

---

### 4. 你没有讲“为什么 OCR 和文本识别并发调用”
你说了并发调用，但没说决策理由。

面试官会追：

- 为什么并发，不是串行？
- 两个结果的数据一致性怎么处理？
- 既然优先 OCR，那文本识别的意义是什么？
- 如果两边结果冲突怎么办？

如果你答不出来，就说明你只是“用了并发”，不是“设计了并发”。

---

### 5. 监控体系太弱
你现在只有：

- 耗时
- 配置字段数
- 解析字段数

对于一个风控解析平台，这远远不够。  
至少还应该考虑：

- 规则命中率
- 第三方降级率
- unknown 类型占比
- 核心字段缺失率
- 不同模板的成功率
- 误提率/人工纠错率
- 第三方调用节省比例
- 失败样本留存量

你可以承认当时没做全，但你必须知道应该怎么补。

---

## 三、如果这题让你重答，应该怎么讲

你可以参考这个更成熟的版本：

> 这个平台前端我把它定位成一个规则运维和调试平台，核心功能包括四块：  
> 第一是字段级规则配置，主要配置 KV 类字段的抽取规则；  
> 第二是表格类规则配置，针对银行流水这类 list/table 结构做行级提取；  
> 第三是文档类型管理，用来维护文档类别和对应规则的绑定关系；  
> 第四是日志和调试能力，支持上传样本直接查看解析结果，便于验证规则效果。  
>
> 后端链路上，我主要负责文档类型路由、OCR/文本识别结果接入、规则执行、降级兜底、日志记录和基础数据清洗。  
> 规则模型上是按文档类型组织，每个类型下有多个目标字段，每个字段可以配置多条规则，规则类型包括正则、关键字和包含匹配，同时支持启停。  
> 一次请求进来后，会先做文档类型识别；识别成功后并发调用 OCR 和文本识别接口，拿到结果后优先基于 OCR 结果进行字段提取，再按规则完成 KV 和表格信息抽取；如果关键字段缺失或完整度不达标，就降级走第三方全量解析链路。  
> 上线后我重点关注了解析耗时、字段命中情况和降级情况，后续如果继续演进，我会补充命中率、误提率和模板维度的效果监控。

这版就明显更像工程师，而不是“边想边说”。

---

## 四、我现在开始打 killer question
这题会直接区分你是“做过项目”还是“能扛高级面”。

## 问题 4：你这个平台最大的技术风险是什么？
我不给你选，你自己答。

你必须从下面这些风险里至少选 **2 个** 展开：

- 上游文档类型识别错了
- OCR 噪声导致正则误提
- 高完整度但低正确率
- 规则越来越多后维护失控
- 新模板泛化能力差
- 降级太频繁导致优化收益不成立
- 前后端配置平台缺少版本治理，容易误发布
- 多人改规则引发线上事故

### 回答要求
你按这个结构答：

1. **我认为最大的两个风险是啥**
2. **为什么这是高风险**
3. **当时你有没有实际处理**
4. **如果现在重做，你会怎么补**

控制在 **2 分钟**。  
别空谈，讲真实项目。

### 18. 候选人回答

目前的最大风险，我觉得就是首先就是文档类型，如果识别出错的话，后续的所有解析都是报废的，也就是说，我高度依赖于第三方文档识别给我返回来的结果来做后续的匹配
还有我觉得就是这个完整性虽然高，但是准确率差需要通过反复的配置来去解决这个正则解析的问题。但是这个正则平台现在涉及的又是比较偏研发的，也就是说后续的维护，比如说来了新模板，又就又就必须由去由研发去配置这个政策，也就是说。这个配置维护的成本也比较大

### 19. 面试官问题 / 追问 / 反馈

这次回答，**方向对了**，你终于抓到两个真风险了：

1. **高度依赖上游文档类型识别**
2. **规则平台维护成本高、研发介入重**

这两个都是真问题，不是假大空。

但是你这次回答依然只到 **“我知道风险是什么”**，还没到 **“我能作为 owner 给出治理方案”**。  
大厂二面不会满足于你只识别问题，他会继续压你：

- 你怎么降依赖？
- 你怎么治理误提？
- 你怎么降低维护成本？
- 你怎么把研发型平台变成业务可运维平台？

---

## 先给你面评式反馈

### 优点
- 你没再讲泛泛而谈的“系统复杂性”，而是讲了真实风险
- 你知道当前方案的核心脆弱点在上游依赖和规则治理
- 你没有硬说“我们都解决了”，这点是对的

### 问题
#### 1. 还是没讲“当时有没有处理”
我明确问了 4 点，其中有一条是：

> 当时你有没有实际处理

你没有正面答。  
这会让面试官觉得你**只能做复盘，不能证明落地**。

#### 2. 没讲“如果重做怎么补”
你只说“需要反复配置”，这不是方案。  
面试官要的是：

- 补什么机制？
- 怎么排优先级？
- 哪些可以工程化？
- 哪些要产品化？

#### 3. “由研发去配正则”这句话其实是在暴露平台没做成
这句很真实，但也很危险。  
如果你不补一句：

> 后续我会把规则配置抽象成更低门槛的模板化配置，并补测试与灰度能力

那面试官会直接判断：

> 这不是平台，这是研发自己维护的一套规则后台。

---

## 这题更成熟的答法应该是这样

你可以参考下面这个版本：

> 我认为这个平台最大的两个风险，第一是过度依赖上游文档类型识别，第二是规则规模扩大后的维护成本和正确率风险。  
>
> 第一个风险在于，一旦上游把文档类型识别错了，后续加载的规则集合就全错了，解析链路会整体失效。当时我们主要做的处理还比较有限，更多是对 unknown 类型做单独记录和补充配置，但本质上还是依赖上游结果。如果现在重做，我会增加类型识别后的二次校验，比如基于关键词、字段特征或版式特征做轻量校验；如果校验不通过，就直接走第三方兜底，而不是继续走错误规则。  
>
> 第二个风险是规则平台会随着模板增多越来越难维护。短期内配置化能提效，但如果规则只能由研发维护，新增模板、调规则、排查误提都会形成瓶颈。当时这一块更多还是研发主导配置，平台化程度不够。如果现在重做，我会补三类能力：第一是规则版本管理和回滚；第二是样本测试集和发布前校验；第三是把常见规则抽象成模板化配置，尽量降低新增模板时的研发介入成本。  
>
> 另外，对于“字段完整度高但实际提错”的问题，我会增加字段级校验和字段间一致性校验，避免把错误结果当成成功结果返回。

你看，这样就从“我知道问题”升级到“我能治理问题”。

---

## 现在继续压你，进入高频 killer 点：指标真实性

你前面一直在讲“秒级到毫秒级”，这种话我非常敏感。  
如果你说不稳，我会直接判你有夸大成分。

## 问题 5：你这个“性能提升”到底怎么量化的？
你必须回答这 6 个点：

1. **你说的毫秒级，指的是哪一段耗时？**
   - 纯规则提取耗时？
   - OCR + 规则总耗时？
   - 端到端接口耗时？

2. **原来 10 秒到 1 分钟，是哪条链路的耗时？**
   - 第三方接口返回？
   - 整体解析？
   - 高峰期波动？

3. **你们怎么测的？**
   - 人工抽样？
   - 接口日志统计？
   - p50 / p95？
   - 单样本测试？

4. **优化收益覆盖多少比例的样本？**
   - 是 5% 的简单单据？
   - 还是 50%？
   - 还是核心大盘？

5. **如果大量请求最后还是降级到了第三方，那你的优化是不是其实不成立？**
   - 你怎么证明它成立？

6. **如果让我质疑你“毫秒级”是吹的，你怎么自证？**

### 回答要求
- 不要再说“差不多”“大概”
- 如果当时没做严谨统计，就直接承认
- 但你要讲清楚**你真实看到的收益来自哪里**

**现在答。**

### 20. 候选人回答

指的是我从拿到这个文本开始，到最终返回结果
原先的几秒到一分钟这个过程，实际上就是他拿到这个文本以后去解析里边儿的有效内容。比如说，我前面说到的银行流水，他要去解析里边儿的。有效的list list里边儿要用什么划线法，还是要用一些位置坐标的匹配法，这个过程是比较耗时的
测的话，就是在接口的各个关键步骤上面买点，看它最每个阶段的耗时，然后最终得到一个总的耗时
我觉得这么做以后，可能会随着规则的丰富以及文档类别的丰富，可能最后会有大概90%的单据，我们这个平台是能cover住的，但剩下10%的可能就是一些比较难解析。或者说是变化非常多的那种模板了
很显然，如果降级到第三方的话，这个结论是不成立的，因为第三方的接口，包括前面你解析的耗时，这样肯定已经达不到毫秒级了
嗯，如果你来怀疑我这个结果的话，因为我觉得我可以从两个方面来去回应这个怀疑。第一，就是他做去做正则匹配其实是个非常快的过程。所以，我能从原来的，需要大模型来识别每个里边儿的具体含义的时候，我我能通过正则直接把它无脑提取出来
第二点就是提取这个list子的时候，我们通过HTML也是做匹配，都是字符串的匹配速度是非常快的，比原先多模态大模型识别里边儿有含义的这种方式要快很多

### 21. 面试官问题 / 追问 / 反馈

停。  
这题你**答出了一部分真实感**，但也暴露了一个大问题：

> 你现在对“性能收益”的表述，**有工程直觉，但缺少严谨口径**。

这在正式面试里非常危险。因为只要你前面说过“毫秒级”，面试官就会默认你必须拿得出**可审计的定义和统计口径**。

---

## 先说这次回答里对的地方

### 1. 你承认了边界
你说：

> 如果降级到第三方，这个结论就不成立

这句是对的，而且很重要。  
说明你至少知道：

- “快”不是全量样本都快
- 规则链路和第三方兜底链路必须分开算
- 不能把所有请求都吹成毫秒级

这是加分项。

### 2. 你讲了测量方法
你说是在关键步骤埋点，看各阶段耗时。  
这比只说“我们感觉快很多”强。

### 3. 你知道快的来源
你本质上表达的是：

- 从重解析 / 重理解链路
- 变成字符串规则匹配链路

这个方向是对的。

---

## 但问题也很明显

### 1. 你把“文本已拿到”和“整条业务链路”混了
你现在的“毫秒级”定义是：

> 从拿到文本开始，到最终返回结果

这就意味着你**没有包含 OCR 获取文本的耗时**。  
那你前面如果跟面试官说：

> 把解析从 10 秒到 1 分钟降到毫秒级

这是容易被打穿的。

因为面试官会立刻反问：

- 你这个“拿到文本开始”是不是已经把最重的一段剔除了？
- 那你优化的是“文本后处理”，不是“整条解析链路”
- 那为什么前面说得像是全链路优化？

所以你以后一定要把口径说清楚：

### 更稳的说法是：
> 我优化的主要是**文本拿到之后的结构化提取阶段**，这一段从原来的秒级处理降低到了接近实时；如果算上前置 OCR 或第三方兜底，端到端耗时不能简单说成毫秒级。

这就稳很多。

---

### 2. 90% cover 这个数字，现在像拍的
你说：

> 可能最后会有大概 90% 的单据能 cover

这里“可能”“大概”“最后”连在一起，面试官会直接警觉：

- 这是上线数据？
- 还是你的预期？
- 还是理想目标？
- 还是某个小样本测试？

如果没有统计依据，千万别说这种大数字。  
更稳的说法应该是：

> 当时还没有形成足够稳定的大盘统计，90% 更接近目标预期，不是我现在能严格证明的线上结论。

或者：

> 在线上观察里，规则稳定命中的主要是结构比较固定的模板，但我当时没有做足够严谨的模板覆盖率统计，所以不敢直接说全量覆盖比例。

---

### 3. 你把“正则快”当成证据，这不够
你现在的自证方式是：

- 正则本来就快
- 字符串匹配本来就快

这只能证明**理论上可快**，不能证明**你的系统实际上快了多少**。

面试官要的证据是：

- 埋点数据
- 样本对比
- 命中链路与降级链路拆分
- p50 / p95
- 高峰期波动

也就是说：

> “原理上快” ≠ “线上结果被证明快”

---

## 如果这题更成熟，你应该这样答

你可以直接记这一版：

> 我前面说的“毫秒级”严格来说不是端到端全链路耗时，而是文本获取完成后，进入规则提取阶段到结果返回这一段的耗时。  
> 原来的慢主要在复杂结构解析，尤其是像银行流水这类 list/table 数据，需要做结构识别和字段映射，处理过程会比较重；而规则链路本质上是基于字符串和结构做匹配，计算成本低很多。  
> 我们当时是通过关键步骤埋点来统计耗时的，能看到规则命中的链路明显快于原有重解析链路。  
> 但如果请求最终降级到了第三方，那这条请求就不能算成毫秒级优化收益，所以这个结论只适用于命中规则链路的样本。  
> 至于覆盖率，我当时没有做非常严谨的全量统计，所以更稳妥的说法是：对结构相对稳定的模板，规则链路有明显提速效果，但不能直接把全量样本都表述成毫秒级。

这就比你刚才稳很多。

---

## 现在我给你上强压题
这是典型二面/三面会问的，专门看你有没有高级工程师思维。

## 问题 6：如果让我现在否掉你的方案，我会这么说
> 你这个东西本质上就是把复杂问题换成了大量规则维护问题。  
> 前期看起来快，后期规则会爆炸；  
> 准确率也不一定稳；  
> 而且高度依赖上游类型识别。  
> 这不一定是平台，可能只是一个短期 patch。

### 你现在站在候选人角度，反驳我。
要求你必须同时回答这 4 点：

1. **为什么这不是一次性 patch，而是有长期价值的能力？**
2. **为什么它不是在替代所有解析链路，而是在做分层治理？**
3. **为什么规则成本虽然高，但在某些场景仍然值得做？**
4. **如果继续演进，你会怎么避免“规则爆炸”？**

### 回答要求
- 先承认问题，不要硬杠
- 再做反驳
- 最后给出演进方向
- 控制在 **2 分钟**

**现在答。**

### 22. 候选人回答

我觉得如果这是站在研发者的视角，它确实是一个一次性的patch。因为随着后期规则的维护，可能对于不停接手的研发也是一种折磨，但是对于站在客户的视角来说的话，能够做到明显的响应提升，我觉得这是有价值的
因为他每一层都有每一层的职责，我们在对于每一层有它对应的一个降级策略
嗯，这就回到第一点，就是如果对于客户来说，他的提升非常明显，对于客户来说，嗯，他。可以很快地得到这个响应结果，我觉得这就是有价值的
但是有一点我要提，在实际的业务中，我们实际上这个平台是是一个to b的产品，To b的客户去拿到这个产品以后，他会对接一些to c的客户，例如一些保险的报销。假如说我有个保险的APP p我作为一个客户to c的客户来说，我会上传我的保单，然后to b采购我们这套系统人会拿会通过调用这个系统，然后返回一些他们认为重要的事情，他们重要这些重要的字段，他们可能采用大模型。审批，或者采用人工审批，然后来返回来。实际上，拿到这个字段的时效性并不是那么高，因为在实际的业务场景中，他客户其实是对于整个流程赔付流程来说，它是能做到7天之以内的，这个可接受程度的
如果后期做的话，我觉得可以对于某些规则，某些模板的某些规则，可以有一个定时任务，定时的去搂，我们可以有一个LU的一个列表的类似的一个规则去淘汰里边儿的正则的正则规则

### 23. 面试官问题 / 追问 / 反馈

停。  
这题你**答崩了**。

我为什么这么说？因为我让你“站在候选人角度反驳我”，结果你一上来先承认：

> 站在研发者视角，它确实是一次性的 patch

这句话在真实面试里几乎等于**主动递刀**。  
面试官本来是在施压测试你，你不应该直接把核心方案定性成短期补丁，除非你后面能立刻把它抬回来，但你没有做到。

---

## 这题你哪里答错了

### 1. 你没有完成“承认问题，但不丢主张”
正确姿势应该是：

- 承认：规则平台有维护成本、边界明显
- 反驳：但它在特定高频、稳定模板场景下，是**有明确 ROI 的分层治理能力**
- 演进：通过模板抽象、版本治理、校验、样本回放来控制规则爆炸

而你现在是：

- 承认问题
- 再次强调“客户觉得快就有价值”
- 但没有建立技术上的长期合理性

这会让面试官觉得你**只会站业务结果，不会站工程长期性**。

---

### 2. 你的“价值证明”不成立
你前面一直在强调“响应更快”，结果这一题里你又说：

> 实际业务里，客户对时效性没那么高，7 天内都能接受

这就直接把你前面的核心价值打掉了。

面试官会立刻抓住：

- 你不是说这个平台的核心价值是响应快吗？
- 那现在又说 7 天内都能接受？
- 那你这个优化的业务必要性到底在哪里？

你这里最大的问题是：**没有区分“终态 SLA”和“链路体验价值”**。

你本来应该说的是：

- 最终赔付流程可以是天级
- 但前置结构化抽取越快，越能提升审核流转、减少人工等待、提高系统吞吐
- 尤其在高频单据场景下，快并不只是 C 端用户体感，而是整条 toB 审核链路的效率收益

这才说得通。

---

### 3. 你的“避免规则爆炸”回答太弱
你最后说了个类似：

> 定时任务 + LRU 淘汰规则

这个想法太浅，而且不对核心。

规则爆炸的核心不是“老规则太多占内存”，而是：

- 规则越来越难理解
- 冲突越来越多
- 新模板接入成本高
- 发布风险高
- 效果不可验证

所以治理重点不是 LRU 淘汰，而是：

- 规则分层与模板化
- 版本管理和灰度发布
- 样本回放测试
- 命中效果监控
- 失效规则识别
- 人工标注闭环

你一说 LRU，面试官会觉得你没有抓住问题本质。

---

## 这题更好的回答模板，我给你一版
你记住这个框架：

> 我承认这个方案有明显边界，如果不治理，后期确实会演变成规则越来越多、维护成本越来越高的问题。  
> 但我不认为它只是一次性 patch，因为它解决的不是“所有解析问题”，而是把解析链路做了分层：对于高频、模板稳定、字段目标明确的场景，规则链路能以更低成本提供更快的结果；对于复杂、变化大的场景，仍然走第三方或更重的解析链路。  
> 所以它的长期价值不在于替代一切，而在于把适合规则化的那部分问题平台化处理，减少对重链路的依赖，提高整体吞吐和成本效率。  
> 规则成本高这一点我认可，但在高频标准化场景下仍然值得做，因为这些场景最适合沉淀为配置能力，收益能覆盖维护成本。  
> 如果继续演进，我会重点做三件事：第一，按文档类型和字段模板做规则抽象，减少重复配置；第二，增加版本管理、灰度和回滚，避免规则发布事故；第三，建立样本回放和效果监控，及时发现失效规则，而不是让规则无限堆积。

这就叫“承认问题，但把方案抬回到工程合理性”。

---

## 到目前为止，我给你一个阶段性判断

### 你的优势
- 项目不是假的，你是做过的
- 你确实有一定 owner 意识，前后端+运维都碰了
- 你知道方案边界，不完全是只会吹结果

### 你的短板
1. **表达结构化不够**
   - 经常想到哪讲到哪
   - 容易重复
   - 术语不稳

2. **技术论证能力偏弱**
   - 能描述现状
   - 但不擅长把“经验方案”包装成“工程方案”

3. **指标口径不严谨**
   - “毫秒级”“90% cover”这种话很危险
   - 容易被面试官抓住

4. **反压能力不足**
   - 面试官质疑你方案时，你容易顺着承认，守不住自己的主张

---

## 现在切到一个你很容易被继续追杀的方向：AI/vibe coding
因为你前面提到自己想追 AI 发展，我现在就要看你是不是只会喊口号。

## 问题 7：结合你这个“规则平台”项目，讲讲你怎么用 AI 工具协助研发
你不要泛泛说“我会用 ChatGPT / Claude 帮我写代码”。

你必须回答这 5 个点：

1. **你会把什么工作交给 AI？**
   - 规则生成？
   - 前端页面脚手架？
   - 后端接口样板？
   - 测试用例？
   - 文档？

2. **你不会把什么工作直接交给 AI？为什么？**

3. **如果让 AI 帮你生成一批正则或解析规则，你怎么防止它胡编？**

4. **你如何把这类 AI 使用沉淀成可复用流程，而不是一次性问答？**

5. **如果让你现在重做这个平台，怎么用 OpenSpec + Claude Code + TDD/SDD 提升交付质量？**

### 回答要求
- 不要空谈“提高效率”
- 结合你的真实项目场景答
- 先说结论，再按 5 点展开
- 控制在 **2 分钟**

**现在答。**

### 24. 候选人回答

现在的话，我几乎会把所有的工作都交给AI来做，尤其是代码占，就是工作内容占比最多的代码开发，在什么情况下我会进入这个过程呢？就是在反复的AI反复的出错，需要人工去排查的时候，以及当接口在反复改的前提下都调不通的情况下，我会在本地debug看一步一步执行到底是会出现了什么问题
我现在还是不会直接把测最终的验收工作交给AI，因为这个验收工作我对于行业一直倡导的TDD原则的话，我觉得还是现在AI能力还是不够，因为考虑到生长环境，测试环境，还有预发环境这种不同环境的复杂性，不同环境有不同的数据，AI在写这个单元测试的时候，可能无法直接连接到预防或者生成那个测试环境去验证这些东西，然后你在本地去测的话，可能更加去方便，而不是交给AI来测
嗯，我会通过提示词的方式，以及给他大量的输入性的文本。这一次我的政则规则表，这个数据的初始化就是交由AI来做的，我会让他分析我给他提供的几百份的发票以及病例解析出来的OCR结果，让他去根据这个反推，我需要的一些字段
嗯，这一部分的话，我现在所用到的工具一般都是Cursor+human agent. Human agent相比于open cloud的话，它是有一套自学习的理论的，它会把我们的问答一个非常大型的一个对话，它会提炼成一个skill，以防我们后续再触发这个事件。但他现在有个问题就是对于这个形成skill的一个条件，现在阈值太低，几乎是5次相关的问答，它就会形成一个对应的skill，以及这些skill它不会有一个淘汰策略，它会。一直存在于那，那就会有一个很明显的问题，就是skill和skill之间会互相的拉踩，就比如说a skill和BC都认为自己好，他们就会互相地去影响最终这个结果
如果我现在重新去做这个东西的话，我一定会去提升它的最终的TD. D这个原则。我一定会把。要做到百分之百的代码覆盖，测试覆盖，然后去依旧提升这个整个代码的健壮性，然后提高对AI的一个信心

### 25. 面试官问题 / 追问 / 反馈

停。

这题你**有真实使用 AI 的痕迹**，但回答仍然有几个很危险的问题。  
如果这是大厂面试，我会判断：

> **你在用 AI，但还没有把 AI 使用方法论讲成“可控的工程流程”。**

而且你一上来那句：

> **“我几乎会把所有的工作都交给 AI 来做”**

这句话风险极大。  
真实面试里，这种表述会立刻触发面试官警觉：

- 你是不是过度依赖 AI？
- 你还有没有独立设计和判断能力？
- 你是不是只会点按钮？
- 代码责任最终谁承担？

---

## 这题你哪里答得对

### 1. 你承认了 AI 的边界
你说最终验收不直接交给 AI，这个方向是对的。  
而且你提到了：

- 不同环境差异
- 测试数据复杂
- 本地 debug 仍然必要

这些都是真问题，不是假话。

### 2. 你讲了一个有真实感的场景
你说会把大量 OCR 结果喂给 AI，让它反推字段和规则。  
这个是有场景感的，比泛泛说“帮我写代码”强。

### 3. 你意识到了 skill 治理问题
你提到：
- skill 生成阈值低
- skill 堆积
- skill 互相干扰

这是很像真实使用中踩过坑的人才会提到的。

---

## 但这题的主要扣分点更严重

### 1. “几乎所有工作都交给 AI”是灾难级表述
你可以高频使用 AI，但绝不能这么说。  
更稳的说法应该是：

> 我会把高重复、结构清晰、容易验证的工作优先交给 AI，比如样板代码、初版规则、测试骨架、文档整理；但方案决策、边界判断、验收和线上责任仍然由我自己把关。

这就叫**会用 AI**。  
你刚才那句容易被理解成：**自己没有主导权**。

---

### 2. 你没有回答“不会交给 AI 的工作”这题的本质
你讲了验收，但还不够。  
真正不该直接交给 AI 的，至少包括：

- 关键架构决策
- 风控口径定义
- 降级策略阈值设定
- 规则正确性验收
- 线上变更审批与发布
- 安全/合规相关判断

因为这些不是“代码能不能生成”的问题，而是**责任归属和业务后果**的问题。

---

### 3. 你没有把“AI 生成正则”讲成闭环
你说给 AI OCR 文本，让它反推规则。这个没问题。  
但面试官最关心的是：

> AI 生成错了怎么办？

你应该讲的是一个完整流程：

1. 给 AI 样本和字段定义  
2. 让 AI 生成候选规则  
3. 在测试集上批量回放  
4. 看命中率、误提率、关键字段通过率  
5. 人工 review 后再发布  
6. 线上灰度观察，再决定是否全量

这才叫**受控使用 AI**。  
你现在只讲了“给它很多文本”，不够。

---

### 4. 你最后又回到了“测试覆盖率 100%”
这句话也很危险。

原因很简单：
- 面试官不信
- 很多系统没必要追求 100%
- 你如果说不出哪些是单测、集成测试、回放测试、契约测试，反而显得不懂测试分层

更好的说法是：

> 我会把测试前移，用单元测试保证规则执行器和解析逻辑的稳定性，用样本回放测试验证模板效果，用集成测试覆盖关键调用链路，而不是空泛追求 100% 覆盖率。

这就稳了。

---

# 这题更成熟的回答模板
你直接记这个版本：

> 如果结合这个规则平台项目来讲，我会把 AI 主要用在四类工作上：第一是前后端样板代码和页面脚手架；第二是规则初稿生成，比如基于 OCR 样本生成候选正则和字段提取逻辑；第三是测试样例和回放脚本生成；第四是文档整理，包括字段说明、规则配置说明和接口文档。  
>
> 但我不会把关键方案决策、规则验收、降级阈值设定和最终发布直接交给 AI，因为这些地方一旦出错，影响的是解析正确率和线上稳定性，责任必须由我自己承担。  
>
> 如果让 AI 生成规则，我不会直接上线，而是让它先生成候选规则，再基于历史样本做批量回放，检查关键字段命中率、误提情况和降级比例，通过人工 review 后再灰度发布。  
>
> 为了避免 AI 使用变成一次性问答，我会把有效流程沉淀成规范，比如固定输入模板、字段定义格式、规则生成提示词、回放测试步骤和发布前检查清单。  
>
> 如果现在重做这个平台，我会用 OpenSpec 先把需求、规则边界、降级策略和验收标准写清楚，再用 Claude Code 做实现，最后用 TDD 保证规则执行器、字段校验和降级逻辑的可测性。重点不是让 AI 代替我开发，而是让 AI 在受控流程里提升交付速度和一致性。

---

# 到这里给你一个阶段性总评

## 总体结论
你不是没东西可讲。  
相反，你这项目里**确实有可挖内容**：

- 智能风控
- 发票/流水解析
- 第三方 OCR/KV 替代
- 配置化规则平台
- 前后端一体
- 降级策略
- toB 场景

这些本来是能打的。

但你当前最大问题不是项目弱，  
而是：

> **你讲项目的方式，撑不起“高级工程师面试”的标准。**

---

## 你的最大优点
- 项目真实
- 做过实事
- 有 owner 痕迹
- 知道方案边界
- 碰过 AI 协作开发，不是纯口嗨

## 你的最大短板
1. **结构化表达差**
2. **术语不稳，口语化太重**
3. **指标口径不严谨**
4. **被质疑时守不住方案**
5. **AI 使用能做，但讲不成工程方法论**

---

# 多维评分
按这一轮表现，我给你一个偏真实的分数：

- 岗位匹配度：**7/10**
- 项目真实性：**8/10**
- ownership：**7/10**
- 技术深度：**6/10**
- 系统设计：**5.5/10**
- 表达结构化：**4.5/10**
- AI 工具协同开发能力：**6/10**
- 抗压表现：**5/10**
- 指标严谨性：**4.5/10**

---

# 高危问题
如果你现在真实去面大厂，最容易挂在这几个点：

1. **“毫秒级”“90% cover”这类指标说法被打穿**
2. **面试官质疑方案时，你容易自己先认输**
3. **AI 使用表述太冒进，像过度依赖**
4. **项目讲得像口头复盘，不像工程设计说明**

---

# 你下一轮前最该修的 3 件事

## 1. 把这个项目整理成固定答案
你必须准备 4 个版本：
- 1 分钟自我介绍版
- 3 分钟项目概述版
- 5 分钟技术深挖版
- 2 分钟风险与反思版

## 2. 把所有指标改成“稳口径”
以后不要轻易说：
- 毫秒级
- 90% 覆盖
- 100% 测试覆盖

改成：
- 规则命中链路接近实时
- 对结构稳定模板有明显提速
- 通过埋点和样本回放验证效果
- 用分层测试保障质量

## 3. 练“承认问题但不丢主张”
你的回答模板应该是：
- 承认边界
- 说明适用场景
- 证明业务价值
- 给出演进方向

---

# 正式面评（写给公司）

**岗位匹配度：一般偏上**  
候选人有真实的风控解析平台研发经历，能讲出配置化规则平台、第三方 OCR/KV 依赖、降级策略、前后端一体开发等内容，项目真实性较高。

**项目真实性与 ownership：较好**  
能说明自己承担了前后端开发、运维、规则平台落地等工作，具备一定 owner 意识。但在架构拆解、治理能力和指标论证上还不够成熟。

**技术能力：中等**  
具备一定工程落地能力，知道如何通过配置化和规则链路优化特定场景性能；但对规则治理、准确率校验、版本管理、效果监控等平台化能力思考不足。

**业务理解：中等偏上**  
对 toB 风控/解析类业务场景有真实接触，理解第三方能力依赖和链路提速诉求，但对业务价值与技术价值的统一表达还不够稳定。

**AI 协同开发潜力：有，但方法论不成熟**  
实际使用过 AI 工具辅助开发，但表述偏“工具使用者”，尚未完全上升到规范化、可审计、可复用的工程流程。

**主要风险点：**
- 表达结构化不足
- 指标口径不严谨
- 方案质疑下防守能力弱
- 高级工程师级别的平台治理能力不足

**最终建议：一般**

---

如果你愿意，我下一轮可以直接进入两种模式之一：

1. **继续高压一面**：我接着追问 Java/数据库/系统设计/408  
2. **专项纠偏**：我帮你把这个“规则平台项目”打磨成一套可背的高质量面试答案

如果按效果看，**我建议你先做专项纠偏**。
