# 算法与代码输出专项

## 元信息

- 面试目录：2025年真实面试
- 大类：算法与代码输出专项
- 来源文件：interview 250444514a3180819cbac6f85ab95b9c.md
- 目标牌组：面试::真实面试::2025-真实面试复盘
- 去重说明：三数之和、快排、二叉树最大宽度、无重复字符最长子串、二分查找等已在算法牌组中覆盖，本文件只补面试现场暴露的缺口题。

## TSV 导出区

字段顺序：

```text
Front<TAB>Back<TAB>Tags<TAB>Source<TAB>Difficulty
```

```tsv
给一个 `List<Integer>`，就地删除所有能被 3 整除的元素，面试中最稳写法是什么？	用显式迭代器：<br><pre><code class="language-java">Iterator&lt;Integer&gt; it = list.iterator();<br>while (it.hasNext()) {<br>    Integer n = it.next();<br>    if (n != null &amp;&amp; n % 3 == 0) {<br>        it.remove();<br>    }<br>}</code></pre><br>关键点：1. 删除当前遍历元素必须用 `Iterator.remove()`；2. `List<Integer>` 可能含 `null`，取模前先判空；3. 这是就地修改原列表，空间 O(1)。	面试 真实面试 算法 List Iterator	贝壳-List删除三整除元素	Hard
为什么不能在增强 for 里直接 `list.remove(x)` 删除元素？	增强 for 底层也是 Iterator。遍历期间直接调用 `list.remove()` 会修改集合的 `modCount`，但 Iterator 里的 `expectedModCount` 没同步更新，下一次 `next()`/`hasNext()` 可能触发 `ConcurrentModificationException`。<br>正确姿势：1. 显式 Iterator + `it.remove()`；2. Java 8+ 可用 `removeIf`；3. 如果要保留原列表，用 stream/filter 生成新列表。	面试 真实面试 算法 List fail-fast	贝壳-List删除三整除元素	Medium
`removeIf` 和 `stream().filter()` 处理“删除三整除元素”有什么区别？	`list.removeIf(n -&gt; n != null &amp;&amp; n % 3 == 0)`：就地删除，修改原列表，返回是否发生删除。<br>`list.stream().filter(n -&gt; n == null || n % 3 != 0).toList()`：返回新列表，不改原列表；注意如果要保留 `null`，filter 条件要显式写 `n == null || ...`。<br>面试表达：先问清楚“是否允许改原列表”和“null 怎么处理”，再选 API。	面试 真实面试 算法 List removeIf Stream	贝壳-List删除三整除元素	Medium
`HelloA/HelloB` 代码输出题题干：先读代码，三种 `main` 分别会输出什么？	<pre><code class="language-java">class HelloA {<br>    public String toString() { return "A"; }<br>    { System.out.println("B"); }<br>    static { System.out.println("C"); }<br>}<br><br>class HelloB extends HelloA {<br>    public String toString() { return "D"; }<br>    { System.out.println("E"); }<br>    static { System.out.println("F"); }<br><br>    public static void main(String[] args) {<br>        System.out.println(new HelloB());<br>        System.out.println(new HelloB());<br>        System.out.println(new HelloA());<br>    }<br>}<br><br>// 变体 2：main 中依次 new HelloA(), new HelloA(), new HelloB()<br>// 变体 3：HelloB.main 是空方法</code></pre><br>先不要背答案，现场按“主动使用类 → 静态初始化 → 每次 new 的实例初始化 → println 调用 toString”推导。	面试 真实面试 JVM 类加载 题干	贝壳-HelloA-HelloB输出顺序	Hard
`HelloA/HelloB` 代码输出题的总口诀是什么？	口诀：父静态 → 子静态 → 父实例字段/实例块 → 父构造器 → 子实例字段/实例块 → 子构造器 → 动态绑定 `toString()`。<br>补充：1. 静态初始化每个类只执行一次；2. 实例代码块每次 `new` 都执行；3. `System.out.println(obj)` 会调用对象运行时类型的 `toString()`；4. `main` 在 `HelloB` 中会触发 `HelloB` 主动使用，因此先初始化父类 `HelloA`，再初始化子类 `HelloB`。	面试 真实面试 JVM 类加载 代码输出	贝壳-HelloA-HelloB输出顺序	Hard
运行 `HelloB.main`，依次执行 `new HelloB()`、`new HelloB()`、`new HelloA()`，输出顺序是什么？	输出为：<br><pre><code class="language-text">C<br>F<br>B<br>E<br>D<br>B<br>E<br>D<br>B<br>A</code></pre><br>推导：第一次主动使用 HelloB，先初始化父类 HelloA 打印 C，再初始化 HelloB 打印 F；第一次 new HelloB 执行父实例块 B、子实例块 E，println 调用 HelloB.toString 输出 D；第二次 new HelloB 静态不再执行，只输出 B E D；new HelloA 输出 B A。	面试 真实面试 JVM 类加载 代码输出	贝壳-HelloA-HelloB输出顺序	Hard
如果在 `HelloB.main` 中先执行 `new HelloA()` 两次，再执行 `new HelloB()`，输出顺序是什么？	输出为：<br><pre><code class="language-text">C<br>F<br>B<br>A<br>B<br>A<br>B<br>E<br>D</code></pre><br>容易错的点：因为 `main` 定义在 HelloB 里，进入 `HelloB.main` 本身就是主动使用 HelloB，所以程序开始时已经先打印 C、F。之后 `new HelloA()` 只执行 HelloA 实例块并调用 HelloA.toString；最后 `new HelloB()` 执行 B、E、D。	面试 真实面试 JVM 类加载 代码输出	贝壳-HelloA-HelloB输出顺序	Hard
如果 `HelloB.main` 是空方法，为什么仍然会输出 `C F`？	因为运行 `HelloB.main` 需要主动使用 HelloB 类。JVM 在调用静态方法前必须完成该类初始化：先初始化父类 HelloA，执行静态块打印 C；再初始化 HelloB，执行静态块打印 F。main 方法体为空只代表没有后续对象创建，不影响类初始化。	面试 真实面试 JVM 类加载 代码输出	贝壳-HelloA-HelloB输出顺序	Medium
代码输出题遇到静态块、实例块、继承和 `toString()` 时，现场如何防止乱？	按表格写：1. 先列“主动使用哪个类”；2. 标记每个类静态块是否已执行；3. 每次 `new` 都画父到子的实例初始化链；4. 最后处理 `println(obj)` 的动态绑定。<br>不要凭感觉读注释，尤其注意 `main` 所在类会先初始化。	面试 真实面试 JVM 类加载 代码输出	贝壳-HelloA-HelloB输出顺序	Medium
设计一个结构：既能按 key 近似 O(1) 查询，又能按 put 顺序遍历，核心结构是什么？	核心就是 HashMap + 双向链表，也就是 LinkedHashMap 的骨架。<br>1. 哈希桶负责按 key 定位节点，平均 O(1) 查询；2. 每个节点额外维护 before/after 指针，串成插入顺序链表；3. put 新 key 时同时插入哈希桶和链表尾；4. 更新旧 key 时只改 value，不改变插入顺序；5. 遍历时走双向链表。	面试 真实面试 集合 LinkedHashMap 数据结构	京东-有序HashMap设计	Medium
手写 OrderedHashMap 时，面试官最容易追问哪些边界？	重点边界：1. hash 取模可能为负数，应使用 `(hash &amp; 0x7fffffff) % table.length` 或扰动后按数组长度取位；2. 负载因子超过阈值要 resize，并重建桶链；3. 删除节点要同时从桶链和顺序链表摘除；4. key 相等用 `Objects.equals`，不是 `==`；5. 线程不安全，多线程要外部同步或换并发容器。	面试 真实面试 集合 LinkedHashMap 数据结构	京东-有序HashMap设计	Hard
```
