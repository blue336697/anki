# 通配符匹配

## 题干
给定一个字符串 s 和一个字符模式 p，实现一个支持 "?" 和 " * " 的通配符匹配。
"?" 可以匹配任何单个字符；" * " 可以匹配任意字符串（包括空字符串）。
匹配需覆盖整个字符串。
![image 2.png](image%202.png)

![image 3.png](image%203.png)

![image 4.png](image%204.png)

![image 5.png](image%205.png)

![image 6.png](image%206.png)

![image 7.png](image%207.png)

![image 8.png](image%208.png)

![image 9.png](image%209.png)

## 复杂度
时间：{{c1::O(m*n)}} — 二维DP
空间：{{c1::O(m*n)}} — dp数组

## 关键技巧
与正则匹配的区别：
1. 正则的 * 修饰前一个字符（如 a*），通配符的 * 独立匹配任意串
2. 通配符更简单：dp[i][j] 表示 p 的前 i 个和 s 的前 j 个是否匹配
3. ? 等价于正则的 .（匹配单个字符）
4. * 转移：不看*（dp[i-1][j]）|| 看*（dp[i][j-1]）
5. 用 | 而非 || 因为需要同时考虑两条转移路径

## 题解(DP)
*的二义性：dp[i-1][j]（*匹配空串）| dp[i][j-1]（*匹配当前字符后继续匹配）。

```java
class Solution {
    public boolean isMatch(String s, String p) {
        int m = s.length(), n = p.length();
        boolean[][] dp = new boolean[n+1][m+1];
        dp[0][0] = true;
        for(int i = 1; i <= n; i++){
            if(p.charAt(i-1) != '*')
                break;
            dp[i][0] = true;
        }
        for(int i = 1; i <= n; i++){
            for(int j = 1; j <= m; j++){
                if(p.charAt(i - 1) == s.charAt(j - 1) || p.charAt(i - 1) == '?')
                    dp[i][j] = dp[i-1][j-1];
                else if(p.charAt(i - 1) == '*')
                    dp[i][j] = dp[i-1][j] | dp[i][j-1];
            }
        }
        return dp[n][m];
    }
}
```
