# 二叉堆与优先队列

## 元信息

- 大类：堆与队列
- 知识点：二叉堆与优先队列
- 目标牌组：算法::数据结构与经典算法::堆与队列::二叉堆与优先队列

## TSV 导出区

字段顺序：

```text
Front<TAB>Back<TAB>Tags<TAB>Source<TAB>Difficulty
```

```tsv
二叉堆是什么？	二叉堆是一棵用数组表示的完全二叉树，满足堆序性质：小根堆中父节点 &lt;= 子节点，大根堆中父节点 &gt;= 子节点。它能高效支持取最小/最大值、插入、删除堆顶，是优先队列常见实现。	算法 数据结构经典算法 堆 优先队列 概念	堆-二叉堆	Medium
堆的数组下标关系是什么？	0 下标数组：父节点 parent=(i-1)/2，左孩子 left=2*i+1，右孩子 right=2*i+2。1 下标数组：父节点 i/2，左孩子 2*i，右孩子 2*i+1。面试写代码时先声明下标体系，避免边界混乱。	算法 数据结构经典算法 堆 下标	堆-二叉堆	Medium
堆的插入和删除堆顶怎么做？	插入：新元素放数组末尾，然后上浮，与父节点比较并交换，直到满足堆序。删除堆顶：用末尾元素覆盖根节点，删除末尾，然后下沉，与更优的孩子交换直到满足堆序。两者高度都是 O(logn)。	算法 数据结构经典算法 堆 操作	堆-二叉堆	Medium
堆的复杂度怎么推导？	peek 堆顶 O(1)。offer/poll 需要沿树高上浮或下沉，完全二叉树高度 O(logn)，所以 O(logn)。heapify 从最后一个非叶子节点向前下沉，总复杂度 O(n)，不是 O(nlogn)，因为底层节点多但下沉高度低。	算法 数据结构经典算法 堆 复杂度	堆-二叉堆	Hard
Java PriorityQueue 使用时有哪些坑？	默认是小根堆。大根堆可用 `(a,b)->Integer.compare(b,a)`，不要写 `b-a` 以免溢出。PriorityQueue 不支持高效删除任意元素或 decrease-key；Dijkstra 常用重复入堆 + 旧距离跳过。自定义对象比较器必须满足传递性。	算法 数据结构经典算法 堆 Java	堆-二叉堆	Hard
看到哪些题型要想到堆？	典型信号：动态维护最大/最小、Top K、数据流中位数、合并 K 个有序链表、任务调度、带权图最短路、最小生成树。若只需要一次性 Top K，也可考虑快选；若需要有序遍历，堆只保证堆顶，不保证整体有序。	算法 数据结构经典算法 堆 例题	堆-二叉堆	Medium
```
