# 红黑树、跳表、B+树对比

## 元信息

- 大类：树结构
- 知识点：红黑树、跳表、B+树对比
- 目标牌组：算法::数据结构与经典算法::树结构::红黑树跳表B加树对比

## TSV 导出区

字段顺序：

```text
Front<TAB>Back<TAB>Tags<TAB>Source<TAB>Difficulty
```

```tsv
红黑树解决什么问题？	红黑树是一种近似平衡二叉搜索树，通过颜色和旋转维持树高 O(logn)，支持查找、插入、删除 O(logn)。Java TreeMap/TreeSet 使用红黑树；HashMap 在桶内链表过长时也会树化为红黑树以降低极端冲突成本。	算法 数据结构经典算法 树结构 红黑树 概念	树结构-对比	Medium
红黑树为什么不是严格平衡？	红黑树追求工程上的综合性能，不要求左右子树高度差严格 &lt;=1，而是用“最长路径不超过最短路径 2 倍”保证 O(logn)。相比 AVL，红黑树查询略弱但插入删除旋转更少，更适合频繁更新的有序 Map/Set。	算法 数据结构经典算法 树结构 红黑树 AVL	树结构-对比	Hard
跳表是什么？	跳表是在有序链表上加多级索引，通过随机层高让查询路径近似二分，查找、插入、删除期望 O(logn)。Redis ZSet 和一些 KV 存储使用跳表，因为实现简单、范围遍历方便、并发改造相对友好。	算法 数据结构经典算法 树结构 跳表 概念	树结构-对比	Medium
跳表和红黑树怎么对比？	两者都支持有序查找 O(logn)。红黑树靠旋转维持平衡，最坏复杂度稳定；跳表靠随机化，期望复杂度 O(logn)，实现更简单，范围扫描天然沿底层链表走。工程上跳表常用于需要顺序遍历、并发友好或实现成本低的场景。	算法 数据结构经典算法 树结构 跳表 红黑树	树结构-对比	Hard
B+树为什么适合数据库索引？	B+树是多路平衡搜索树，非叶子节点只存 key 和子指针，叶子节点存数据或主键并按顺序链表连接。多路分叉让树高很低，减少磁盘/页 IO；叶子链表支持范围查询。这比二叉树更适合磁盘页和数据库索引。	算法 数据结构经典算法 树结构 B加树 数据库	树结构-对比	Hard
B树和B+树核心区别是什么？	B树的内部节点也可存数据，命中内部节点可提前返回；B+树数据只在叶子节点，内部节点只做导航。B+树查询路径更稳定，单页能放更多索引 key，树更矮，并且叶子链表天然支持范围扫描，所以数据库索引更常用 B+树。	算法 数据结构经典算法 树结构 B树 B加树	树结构-对比	Medium
为什么 MySQL 索引不用 HashMap 或红黑树做主力？	Hash 索引不支持范围查询和有序扫描；红黑树分叉少，数据量大时树高高，磁盘随机 IO 多；B+树按页组织，多路分叉降低高度，叶子有序链表适合范围查询和排序。因此 InnoDB 主力索引结构是 B+树。[[img:tree_index_comparison.svg]]	算法 数据结构经典算法 树结构 索引 对比	树结构-对比	Hard
```
