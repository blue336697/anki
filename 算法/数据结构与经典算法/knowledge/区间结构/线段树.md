# 线段树

## 元信息

- 大类：区间结构
- 知识点：线段树
- 目标牌组：算法::数据结构与经典算法::区间结构::线段树

## TSV 导出区

字段顺序：

```text
Front<TAB>Back<TAB>Tags<TAB>Source<TAB>Difficulty
```

```tsv
线段树解决什么问题？	线段树用于维护数组区间信息，支持区间查询和单点/区间更新。典型信息包括区间和、最大值、最小值、最大子段和等。它把区间递归拆成左右子区间，用树节点保存区间聚合结果。	算法 数据结构经典算法 区间结构 线段树 概念	区间-线段树	Medium
线段树为什么是 O(logn)？	线段树高度是 O(logn)。单点更新只影响从叶子到根的一条路径，所以 O(logn)。区间查询会把查询区间拆成 O(logn) 个不相交节点区间，因此也是 O(logn) 到 O(logn + k) 级别，具体看返回内容。	算法 数据结构经典算法 区间结构 线段树 复杂度	区间-线段树	Hard
线段树节点维护什么？	节点至少维护区间 [l,r] 和聚合值 sum/max/min。数组实现常用 tree[4*n] 存值，递归参数带 l/r，不一定显式存节点对象。区间更新还需要 lazy 标记，用来推迟对子节点的更新。	算法 数据结构经典算法 区间结构 线段树 结构	区间-线段树	Medium
线段树核心模板有哪些函数？	核心函数：build(idx,l,r) 建树；query(idx,l,r,ql,qr) 查询；update(idx,l,r,pos,val) 单点更新；pushUp(idx) 用左右孩子重算当前节点；区间更新还需要 pushDown(idx) 下推 lazy 标记。	算法 数据结构经典算法 区间结构 线段树 模板	区间-线段树	Hard
lazy 标记解决什么问题？	lazy 标记用于区间更新。如果更新 [L,R] 时某个节点区间被完全覆盖，就只更新该节点聚合值并记录 lazy，不立刻递归更新所有子节点。等后续查询/更新需要访问子节点时再 pushDown。这样区间更新仍可保持 O(logn)。	算法 数据结构经典算法 区间结构 线段树 lazy	区间-线段树	Hard
线段树和树状数组怎么选？	树状数组代码短、常数小，适合前缀和类问题：单点更新 + 区间和查询，或简单差分。线段树更通用，适合区间最大/最小、复杂合并信息、区间更新、动态开点。能用树状数组时通常更轻；需求复杂时用线段树。[[img:segment_tree_bit.svg]]	算法 数据结构经典算法 区间结构 线段树 对比	区间-线段树	Medium
```
