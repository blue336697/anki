# 树状数组

## 元信息

- 大类：区间结构
- 知识点：树状数组
- 目标牌组：算法::数据结构与经典算法::区间结构::树状数组

## TSV 导出区

字段顺序：

```text
Front<TAB>Back<TAB>Tags<TAB>Source<TAB>Difficulty
```

```tsv
树状数组解决什么问题？	树状数组 Fenwick Tree 主要解决动态前缀和：支持单点更新和前缀和查询，进而支持区间和查询。它用 lowbit 把前缀拆成若干段，代码比线段树更短，常用于逆序对、排名统计、动态区间和。[[img:segment_tree_bit.svg]]	算法 数据结构经典算法 区间结构 树状数组 概念	区间-树状数组	Medium
lowbit 是什么？	lowbit(x)=x&-x，表示 x 的二进制最低位 1 对应的值。树状数组中 tree[i] 管理长度为 lowbit(i) 的一段区间。查询时 i -= lowbit(i) 向前跳；更新时 i += lowbit(i) 更新所有包含该位置的段。	算法 数据结构经典算法 区间结构 树状数组 lowbit	区间-树状数组	Hard
树状数组 Java 模板怎么写？	模板使用 1 下标：<br><pre><code class="language-java">class BIT {<br>    int n; long[] tree;<br>    BIT(int n) { this.n = n; tree = new long[n + 1]; }<br>    int lowbit(int x) { return x &amp; -x; }<br>    void add(int i, long delta) {<br>        for (; i &lt;= n; i += lowbit(i)) tree[i] += delta;<br>    }<br>    long sum(int i) {<br>        long res = 0;<br>        for (; i &gt; 0; i -= lowbit(i)) res += tree[i];<br>        return res;<br>    }<br>    long rangeSum(int l, int r) { return sum(r) - sum(l - 1); }<br>}</code></pre>	算法 数据结构经典算法 区间结构 树状数组 模板	区间-树状数组	Hard
树状数组复杂度怎么推导？	每次 add 或 sum 都通过 lowbit 跳过一段二进制区间，最多跳 O(logn) 次，因此单点更新和前缀查询都是 O(logn)。空间 O(n)。构建可逐个 add 为 O(nlogn)，也有 O(n) 建树方式。	算法 数据结构经典算法 区间结构 树状数组 复杂度	区间-树状数组	Medium
树状数组有哪些典型题？	典型：数组动态区间和、逆序对、计算右侧小于当前元素个数、离散化后的频次前缀和、二维前缀动态更新。看到“动态修改 + 前缀/区间和 + 排名统计”，可以优先想 BIT。	算法 数据结构经典算法 区间结构 树状数组 例题	区间-树状数组	Medium
树状数组的边界是什么？	普通树状数组适合可逆的前缀聚合，如 sum，因为区间查询可用 prefix(r)-prefix(l-1)。对于 max/min 这类不可逆操作，普通 BIT 不适合任意区间查询。区间更新可结合差分 BIT，但复杂合并信息通常交给线段树。	算法 数据结构经典算法 区间结构 树状数组 边界	区间-树状数组	Hard
```
