# 拓扑排序

## 元信息

- 大类：图论
- 知识点：拓扑排序
- 目标牌组：算法::数据结构与经典算法::图论::拓扑排序

## TSV 导出区

字段顺序：

```text
Front<TAB>Back<TAB>Tags<TAB>Source<TAB>Difficulty
```

```tsv
拓扑排序解决什么问题？	拓扑排序用于有向无环图 DAG，给出一种线性顺序，使得每条有向边 u->v 中 u 都排在 v 前面。它常用于课程依赖、任务编排、构建依赖、审批/流程前置条件等场景。若图中有环，则不存在合法拓扑序。	算法 数据结构经典算法 图论 拓扑排序 概念	图论-拓扑排序	Medium
Kahn 算法的核心流程是什么？	流程：1. 统计每个点入度；2. 将入度为 0 的点入队；3. 每次弹出一个点加入结果；4. 遍历它的出边，把后继节点入度减 1；5. 后继入度变 0 时入队；6. 最后若结果数量等于节点数，说明存在拓扑序；否则图中有环。[[img:topological_sort_kahn.svg]]	算法 数据结构经典算法 图论 拓扑排序 Kahn	图论-拓扑排序	Medium
拓扑排序 Java 模板怎么写？	模板：<br><pre><code class="language-java">int[] indeg = new int[n];<br>List&lt;Integer&gt;[] graph = new List[n];<br>Deque&lt;Integer&gt; q = new ArrayDeque&lt;&gt;();<br>for (int i = 0; i &lt; n; i++) if (indeg[i] == 0) q.offer(i);<br>List&lt;Integer&gt; order = new ArrayList&lt;&gt;();<br>while (!q.isEmpty()) {<br>    int u = q.poll();<br>    order.add(u);<br>    for (int v : graph[u]) {<br>        if (--indeg[v] == 0) q.offer(v);<br>    }<br>}<br>return order.size() == n ? order : Collections.emptyList();</code></pre>如果只判断能否完成课程，最后看 `order.size() == n`。	算法 数据结构经典算法 图论 拓扑排序 模板	图论-拓扑排序	Hard
拓扑排序复杂度怎么推导？	建图和统计入度需要遍历所有点和边：O(V+E)。Kahn 过程中每个点最多入队出队一次，每条边只在其起点出队时被处理一次，因此也是 O(V+E)。空间 O(V+E)：邻接表 O(V+E)，入度数组和队列 O(V)。	算法 数据结构经典算法 图论 拓扑排序 复杂度	图论-拓扑排序	Medium
拓扑排序如何检测环？	如果有环，环上的点入度无法被全部削减到 0，因此不会全部进入队列。Kahn 算法结束后，若已输出节点数小于总节点数，说明存在环。DFS 版本则用三色标记：0 未访问、1 访问中、2 已完成；DFS 时遇到访问中节点表示有环。	算法 数据结构经典算法 图论 拓扑排序 环检测	图论-拓扑排序	Hard
看到哪些题型要想到拓扑排序？	典型信号：1. 存在先修/依赖/前置关系；2. 问能否完成所有任务；3. 要输出一个满足依赖的顺序；4. 检查有向图是否有环。例题：课程表、课程表 II、项目构建顺序、任务调度、字典序外星文字母顺序。	算法 数据结构经典算法 图论 拓扑排序 例题	图论-拓扑排序	Medium
拓扑排序和并查集在环检测上有什么区别？	并查集常用于无向图加边成环判断；拓扑排序用于有向图依赖环检测。课程依赖 A->B 是方向敏感的，不能用普通并查集判断；普通并查集只知道连通分量，不知道方向和先后约束。面试先看“有向/无向”，再选工具。	算法 数据结构经典算法 图论 拓扑排序 对比	图论-拓扑排序	Hard
```

