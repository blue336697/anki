# Deque 经典算法应用

## 1. BFS 广度优先搜索
Deque 作为标准队列，逐层遍历：
```java
Deque<TreeNode> queue = new ArrayDeque<>();
queue.offer(root);
while (!queue.isEmpty()) {
    TreeNode node = queue.poll();
    if (node.left != null)  queue.offer(node.left);
    if (node.right != null) queue.offer(node.right);
}
```

## 2. 简化路径（71）
用 Deque 当栈，处理 Unix 路径：
```java
public String simplifyPath(String path) {
    Deque<String> stack = new ArrayDeque<>();
    for (String part : path.split("/")) {
        if (part.isEmpty() || part.equals(".")) continue;
        if (part.equals("..")) {
            if (!stack.isEmpty()) stack.pop();
        } else {
            stack.push(part);
        }
    }
    StringBuilder sb = new StringBuilder();
    for (String s : stack) sb.insert(0, "/" + s);
    return sb.length() == 0 ? "/" : sb.toString();
}
```

## 3. 回文检查（双端比较）
```java
public boolean isPalindrome(String s) {
    Deque<Character> dq = new ArrayDeque<>();
    for (char c : s.toLowerCase().toCharArray()) {
        if (Character.isLetterOrDigit(c)) dq.offerLast(c);
    }
    while (dq.size() > 1) {
        if (dq.pollFirst() != dq.pollLast()) return false;
    }
    return true;
}
```

## 4. 接雨水（42）
用 Deque 维护单调递减栈：
```java
public int trap(int[] height) {
    Deque<Integer> stack = new ArrayDeque<>();
    int water = 0;
    for (int i = 0; i < height.length; i++) {
        while (!stack.isEmpty() && height[i] > height[stack.peek()]) {
            int bottom = stack.pop();
            if (stack.isEmpty()) break;
            int left = stack.peek();
            int w = i - left - 1;
            int h = Math.min(height[left], height[i]) - height[bottom];
            water += w * h;
        }
        stack.push(i);
    }
    return water;
}
```

## 5. 二叉树反序列化 (297)
```java
public TreeNode deserialize(String data) {
    if (data.equals("null")) return null;
    String[] nodes = data.split(",");
    TreeNode root = new TreeNode(Integer.parseInt(nodes[0]));
    Deque<TreeNode> q = new ArrayDeque<>();
    q.offer(root);
    int i = 1;
    while (!q.isEmpty() && i < nodes.length) {
        TreeNode node = q.poll();
        if (!nodes[i].equals("null")) {
            node.left = new TreeNode(Integer.parseInt(nodes[i]));
            q.offer(node.left);
        }
        i++;
        if (i < nodes.length && !nodes[i].equals("null")) {
            node.right = new TreeNode(Integer.parseInt(nodes[i]));
            q.offer(node.right);
        }
        i++;
    }
    return root;
}
```

## Deque 算法场景速查

| 场景 | Deque 角色 | 关键 API |
|------|------|------|
| BFS 层序遍历 | 标准队列 | offer + poll |
| DFS 迭代 | 栈 | push + pop |
| 滑动窗口最值 | 单调队列 | peekFirst + pollFirst + pollLast + offerLast |
| 接雨水 | 单调栈 | peek + push + pop |
| 路径简化 | 栈 | push + pop |
| 回文检查 | 双端队列 | pollFirst + pollLast |
| 括号匹配 | 栈 | push + pop |
| 拓扑排序 | 队列 | offer + poll |
| 表达式计算 | 栈 | push + pop |

## 学习建议
1. 先死记 {{c1::push/pop/peek（栈模式）和 offer/poll/peek（队列模式）}} 两组 API
2. 理解 {{c2::offerFirst/offerLast/pollFirst/pollLast}} 的完整能力
3. 重点掌握 {{c3::单调队列（滑动窗口最大值）}}，这是 Deque 算法题的巅峰应用
4. {{c4::永远用 ArrayDeque，不用 Stack 和 LinkedList（当队列用）}}
