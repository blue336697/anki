# 字符串的排列

## 题干
输入一个字符串，打印出该字符串中字符的所有排列。你可以以任意顺序返回这个字符串数组。字符串中可能有重复字符。

## 回溯-选择列表
选择列表 = {{c1::s 的字符数组}}
与全排列 II 完全相同的模板：{{c1::排序+used数组+同层去重}}

## 回溯-终止+剪枝
终止条件：{{c1::sb.length() == ch.length}} → 加入结果
剪枝策略：{{c1::i>0 && ch[i]==ch[i-1] && !used[i-1]}} → continue
与全排列 II 完全一样，只是处理 char[] 而非 int[]

## 复杂度
时间：{{c1::O(n!)}} — n个字符的全排列
空间：{{c1::O(n)}} — 递归深度+used数组

## 题解(DFS+剪枝)
与全排列 II 完全相同模板，区别仅在于处理 char[] vs int[]。

```java
class Solution {
    List<String> res = new ArrayList<>();
    public String[] permutation(String s) {
        int len = s.length();
        if (len <= 0)
            return null;
        StringBuilder sb = new StringBuilder();
        boolean[] used = new boolean[len];
        char[] ch = s.toCharArray();
        Arrays.sort(ch);
        dfs(ch, sb, used);
        return res.toArray(new String[res.size()]);
    }

    public void dfs(char[] ch, StringBuilder sb, boolean[] used) {
        if (sb.length() == ch.length) {
            res.add(sb.toString());
            return;
        }
        for (int i = 0; i < ch.length; i++) {
            if (i > 0 && ch[i] == ch[i - 1] && used[i - 1] == false)
                continue;
            if (used[i] == false) {
                used[i] = true;
                sb.append(ch[i]);
                dfs(ch, sb, used);
                sb.deleteCharAt(sb.length() - 1);
                used[i] = false;
            }
        }
    }
}
```

## 对比
与全排列 II 完全相同的模板，区别仅在于处理 char[] vs int[]。
剪枝用 Set 去重更直观但更慢，排序+used才是最优解。
