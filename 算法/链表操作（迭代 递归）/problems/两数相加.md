# 两数相加

## 题干
给定两个非空的链表，代表两个非负整数。它们每位数字都是按逆序存储。
将两数相加，以相同链表形式返回和。
(2->4->3) 代表 342。
![image 8.png](image%208.png)

![image 9.png](image%209.png)

## 复杂度
时间：O(max(m,n))
空间：O(max(m,n)) — 结果链表

## 关键技巧
1. dummy节点 + cur指针构建结果链
2. carry 标志位进位
3. 三条件循环：l1!=null || l2!=null || carry!=0
4. 每次取 l1/l2 的 val（null 则取 0），加 carry
5. carry = sum / 10，新节点 = sum % 10

## 题解
逐位相加+进位标志，空位补0，三条件循环确保最后进位也被处理。

```java
class Solution {
    public ListNode addTwoNumbers(ListNode l1, ListNode l2) {
        ListNode prev = new ListNode(0);
        int carry = 0;
        ListNode cur = prev;
        while (l1 != null || l2 != null) {
            int x = l1 != null ? l1.val : 0;
            int y = l2 != null ? l2.val : 0;
            int sum = x + y + carry;
            carry = sum / 10;
            sum = sum % 10;
            cur.next = new ListNode(sum);
            cur = cur.next;
            if (l1 != null) {
                l1 = l1.next;
            }
            if (l2 != null) {
                l2 = l2.next;
            }
        }
        if (carry == 1) {
            cur.next = new ListNode(carry);
        }
        return prev.next;
    }
}
```

## 对比
与 链表求和 是同一道题的不同表述，解法相同
与 两数相加II 的区别：II是正向存储需先反转
