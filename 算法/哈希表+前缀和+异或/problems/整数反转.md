# 整数反转

## 题干
给你一个 32 位的有符号整数 x，返回将 x 中的数字部分反转后的结果。如果反转后整数超过 32 位有符号整数的范围 [-2^31, 2^31-1]，就返回 0。
![image 6.png](image%206.png)

![image 7.png](image%207.png)

## 复杂度
时间：{{c1::O(log n)}} — 数字位数
空间：{{c1::O(1)}}

## 关键技巧
溢出判断公式（Java int 范围 [-2147483648, 2147483647]）：
正溢出：res > 214748364 || (res == 214748364 && tmp > 7)
负溢出：res < -214748364 || (res == -214748364 && tmp < -8)
原理：214748364 是 MAX/10，7 是 MAX%10。必须在乘10之前判断。

## 题解(取余+溢出判断)
每次取末尾数字tmp=x%10，在res*10+tmp之前判断溢出。

```java
class Solution {
    public int reverse(int x) {
        int res = 0;
        while(x != 0) {
            int tmp = x % 10;
            if (res > 214748364 || (res == 214748364 && tmp > 7)) {
                return 0;
            }
            if (res < -214748364 || (res == -214748364 && tmp < -8)) {
                return 0;
            }
            res = res * 10 + tmp;
            x /= 10;
        }
        return res;
    }
}
```
