# JDK简要进化史（11~17）

type: Post
status: Published
date: 2023/07/25
summary: JDK简要进化史（11~17）
tags: Java
category: 技术分享

# **JDK11查缺补漏（LTS）**

### **Lambda中支持var变量**

前面刚说到JDK10支持var变量，但对此的限制很多，这不立马又增强了用法——没懂下面两种的区别

```java
Arrays.asList("1", "2", "3").stream().forEach(i->System.out.println(i+"1"));
Arrays.asList("1", "2", "3").stream().forEach((var i)->System.out.println(i+"1"));
```

### **HTTP Client相关API得到优化**

在这个API首次出现在JDK9，推出之后异常难用，这也给了很多像`okhttp`、`restTemplate`和`feign`这样的第三方库极大的发挥空间，几乎是没人用原生的`HTTP`客户端的。

> 从java9的jdk.incubator.httpclient模块迁移到java.net.http模块，包名由jdk.incubator.http改为java.net.http
> 

```java
@Test
public void testHttp() throws IOException, InterruptedException {
    //构建request
    var request = HttpRequest.newBuilder()
            .uri(URI.create("https://www.baidu.com"))
            .GET()
            .build();
    //构建client
    var client = HttpClient.newHttpClient();
 
    // 同步请求
    HttpResponse<String> response = client.send(request, HttpResponse.BodyHandlers.ofString());
    System.out.println(response.body());
 
    // 异步请求
    client.sendAsync(request, HttpResponse.BodyHandlers.ofString())
            .thenApply(HttpResponse::body)
            .thenAccept(System.out::println);
}
```

### **String-API增强**

```java
//判断字符串是否为空字符串
" ".isBlank();//true
"".isBlank();//true
//去除字符串首尾空格
" Java ".strip();// "Java"
//去除字符串首部空格
" Java ".stripLeading();   // "Java "
//去除字符串尾部空格
" Java ".stripTrailing();  // " Java"
//重复字符串多少次
"Java".repeat(3);             // "JavaJavaJava"
"Java\n".repeat(4);          // "Java\nJava\nJava\nJava\n"
//返回由换行符分隔的字符串集合流
"A\nB\nC".lines().count();    // 3
"A\nB\nC".lines().collect(Collectors.toList()); // ["A", "B", "C"]
"A\nBC".lines().collect(Collectors.toList());   // ["A", "BC"]
```

### **读写文件**

对`Files`类增加了`writeString`**和**`readString`两个静态方法，可以直接把`String`写入文件，或者把整个文件读出为一个`String`，**这两个方法可以大大简化读取配置文件之类的问题**。

```java
@Test
public void testReadOrWrite() throws IOException {
    Files.writeString(
            Path.of("./", "tmp.txt"), // 路径
            "hello, jdk11 files api", // 内容
            StandardCharsets.UTF_8); // 编码
    String s = Files.readString(
            Paths.get("./tmp.txt"), // 路径
            StandardCharsets.UTF_8); // 编码
}
```

![image.png](JDK%E7%AE%80%E8%A6%81%E8%BF%9B%E5%8C%96%E5%8F%B2%EF%BC%8811~17%EF%BC%89/image.png)

### **ZGC（可伸缩低延迟垃圾收集器）**

[权威使用分析](https://tech.meituan.com/2020/08/06/new-zgc-practice-in-meituan.html)

### **Optional增强**

- 新增了`isEmpty()`方法来判断指定的 `Optional` 对象是否为空。

```java
var op = Optional.empty();
System.out.println(op.isEmpty());//判断指定的 Optional 对象是否为空

```

# **JDK12~16的查缺补漏**

## JDK12

### **String-Api的增强**

- indent()：字符串缩减，接受一个`int`型的输入
- transform()：接受一个转换函数，转变指定字符串

```java
String text = "Java";
// 缩进 4 格
text = text.indent(4);
System.out.println(text);
text = text.indent(-10);
System.out.println(text);
 
//输出
    Java
 
Java
 
String result01 = "foo".transform(input -> input + " bar");
System.out.println(result01); // foo bar
String transform = "hello jdk12".transform(s -> s.toUpperCase());
System.out.println(transform); // HELLO JDK12
```

### **全新Switch写法**

**引入了->来表达结果的走向，这里不是Lambda的意思**；我的评价是还就那个越来越Python&Go化。在JDK14中正式推出，不需要再加入参数

```java
@Test
    public void testSwitch(){
/*---------------------------------------旧-----------------------------------------------*/
        switch (day) {
            case MONDAY:
            case FRIDAY:
            case SUNDAY:
                System.out.println(6);
                break;
            case TUESDAY:
                System.out.println(7);
                break;
            case THURSDAY:
            case SATURDAY:
                System.out.println(8);
                break;
            case WEDNESDAY:
                System.out.println(9);
                break;
            default:
                throw new IllegalStateException("Unexpected value: " + day);
        }
/*---------------------------------------新-----------------------------------------------*/
        switch (day) {
            case MONDAY, FRIDAY, SUNDAY -> System.out.println(6);
            case TUESDAY                -> System.out.println(7);
            case THURSDAY, SATURDAY     -> System.out.println(8);
            case WEDNESDAY              -> System.out.println(9);
            default -> throw new IllegalStateException("Unexpected value: " + day);
        }
 
        // 还可以有返回值
        //'switch' expression does not cover all possible input values
        int numLetters = switch (day) {
            case MONDAY, FRIDAY, SUNDAY -> 6;
            case TUESDAY                -> 7;
            case THURSDAY, SATURDAY     -> 8;
            case WEDNESDAY              -> 9;
            default -> throw new IllegalStateException("Unexpected value: " + day);
        };
    }
```

### **Files增强（比较）**

- public static long mismatch(Path path, Path path2) throws IOException：方法用于比较两个文件，**并返回第一个不匹配字符的位置，如果文件相同则返回 -1L**

```java
@Test
public void testCompare() throws IOException {
    //hello, jdk11 files api
    //hello, jdk12 files api
    long mismatch = Files.mismatch(Path.of("./", "tmp01.txt"), Path.of("./", "tmp02.txt"));
    System.out.println(mismatch);   //11
}
```

### **NumberFormat增加了对以紧凑格式格式化数字的支持**

`NumberFormat`增加了以紧凑格式格式化数字的支持。 紧凑的数字格式是指数字的简短形式或易于理解的形式。 例如，在`en_US`语言环境中，根据`NumberFormat.Style`指定的样式，可以将`1000`格式化为`1K`，将`1000000`格式化为`1M`。

```java
NumberFormat fmt = NumberFormat.getCompactNumberInstance(Locale.CHINA, NumberFormat.Style.LONG);
System.out.println(fmt.format(1000000000));  //最高10亿
NumberFormat fmt01 = NumberFormat.getCompactNumberInstance(Locale.US, NumberFormat.Style.LONG);
System.out.println(fmt01.format(1000000000));  //1 billion
```

### **G1垃圾回收器的优化**

- 优化回收未使用已分配的内存
- 将回收目标简单分为两种：一种是必须回收完的目标；第二种是可终止的回收，在回收过程中后者是可以被终止来提高提顿时间的目标

### **instanceof 模式匹配**

`instanceof` 主要在类型强转前探测对象的具体类型。在之前的版本中，我们需要显示地对对象进行类型转换。

- 需要确定的类型判断才能使用
- 作用域的范围需要注意

```java
   @Test
   public void testInstanceOf() {
       /*-------------------旧----------------------*/
       Object obj01 = "我是字符串";
       if (obj01 instanceof String) {
           String str = (String) obj01;
           System.out.println(str);
       }
       /*-------------------新----------------------*/
       Object obj02 = "hello";
       if (obj02 instanceof String str) {
           System.out.println(str.toUpperCase());
       } else {
           System.out.println(obj02);
       }
      Object obj03 = "hell";
       if (obj03 instanceof String str && str.length() > 5) {
           System.out.println(str.toUpperCase());
       } else {
           System.out.println(obj03);
       }    
   }
```

## JDK13

### **字符串块（文本块）**

Java 13 支持两个 `"""` 符号中间的任何内容都会被解释为字符串的一部分，包括换行符，优化一些格式化表达，将重复冗余的工作交给JDK。

```java
@Test
public void testStringArea(){
    String html01 = "<html>\n" +
            "    <body>\n" +
            "        <p>Hello, world</p>\n" +
            "    </body>\n" +
            "</html>\n";
    String html02 = """
          <html>
              <body>
                  <p>Hello, world</p>
              </body>
          </html>
          """;
    String json01 ="{\n" +
            "   \"name\":\"mkyong\",\n" +
            "   \"age\":38\n" +
            "}\n";
    String json02 = """
            {
               "name":"mkyong",
               "age":38
            }
            """;
    String query01 = "SELECT `EMP_ID`, `LAST_NAME` FROM `EMPLOYEE_TB`\n" +
            "WHERE `CITY` = 'INDIANAPOLIS'\n" +
            "ORDER BY `EMP_ID`, `LAST_NAME`;\n";
    String query02 = """
           SELECT `EMP_ID`, `LAST_NAME` FROM `EMPLOYEE_TB`
           WHERE `CITY` = 'INDIANAPOLIS'
           ORDER BY `EMP_ID`, `LAST_NAME`;
           """;
}
```

### **增强 Switch(引入 yield 关键字到 Switch 中)**

`yield`，主要用于返回一个值；同理也是在JDK14中正式推出

> yield和 return 的区别在于：return 会直接跳出当前循环或者方法，而 yield 只会跳出当前 Switch 块，同时在使用 yield 时，需要有 default 条件
> 

```java
@Test
public String testSwitchYield(){
    return switch (name) {
        case "Java": yield "object-oriented, platform independent and secured";
        case "Ruby": yield "a programmer's best friend";
        default: yield name +" is a good language";
    };
}
```

### **其他：**

- ZGC优化已分配但未位置的内存回收速度
- 动态CDS存档
- FileSystems的三个新方法
- SocketAPI重构

## JDK14

### **空指针精准提示**

通过 JVM 参数中添加`-XX:+ShowCodeDetailsInExceptionMessages`，可以在空指针异常中获取更为详细的调用信息，更快的定位和解决问题。尤其是在链式调用中，可以很明确的看到哪里出错。在JDK17中不需要加VM参数

```java
    public void testNPE(){
        User user = new User();
        String LeaderName = user.getCompany().getDepartment().getLeaderName();
    }
/*-----------------------旧---------------------------*/
java.lang.NullPointerException
    at com.lhj.crud.MyTests.testNPE(MyTests.java:468)
/*-----------------------新---------------------------*/
java.lang.NullPointerException: Cannot invoke "com.lhj.crud.dao.entity.Company.getDepartment()" because the return value of "com.lhj.crud.dao.entity.User.getCompany()" is null
 
    at com.lhj.crud.MyTests.testNPE(MyTests.java:493)
```

---

### **Record关键字**

这个关键字可以理解为官方原生的Lombok，但是更加适用于保存那种不需要更改数据的类；用法是只需要声明属性，就可以在获得属性的访问方法，以及 **`toString()`，`hashCode()`, `equals()`**方法——》相当于打上了**`@Getter,@ToString,@EqualsAndHashCode。对比@Data注解少了个@Setter和@`**RequiredArgsConstructor**`的作用`**

> @RequiredArgsConstructor会将类的每一个final字段或者non-null字段生成一个构造方法
> 
- **构造函数** --- 默认record类具有全部属性的构造函数，不具备无参构造
- **属性** --- 属性最好在类声明时就写在括号中，如果想在body中单独声明，仅支持static属性，不建议这样
- **不能被继承**

```java
/**
 * 这个类具有两个特征
 * 1. 所有成员属性都是final
 * 2. 全部方法由构造方法，和两个成员属性访问器组成（共三个）
 * 那么这种类就很适合使用record来声明
 */
final class Rectangle implements Shape {
    final double length;
    final double width;
 
    public Rectangle(double length, double width) {
        this.length = length;
        this.width = width;
    }
 
    double length() { return length; }
    double width() { return width; }
}
/**
 * 1. 使用record声明的类会自动拥有上面类中的三个方法
 * 2. 在这基础上还附赠了equals()，hashCode()方法以及toString()方法
 * 3. toString方法中包括所有成员属性的字符串表示形式及其名称
 */
record Rectangle(float length, float width) { }
```

注意：

- 无法与lombok的大多数注解配合使用
- 在1.18.16之后可以与@Builder注解配合使用

### **字符串块增强**

引入两个新的转义字符

- `\` : 表示行尾，不引入换行符
- `\s`：表示单个空格

### **其他**

- 正式移除CMS

## JDK15

### **CharSequence增强**

添加了一个默认方法 `isEmpty()` 来判断字符序列为空，如果是则返回 true。

### **TreeMap增强**

添加一些普通map中就存在的方法

- `putIfAbsent()`
- computeIfAbsent()
- computeIfPresent()
- compute()
- merge()

### **ZGC**

正式可以使用，但默认还是G1

| 1 | `$ java -XX:+UseZGC className` |
| --- | --- |

### **EdDSA(数字签名算法)**

新加入了一个安全性和性能都更强的基于 Edwards-Curve Digital Signature Algorithm （EdDSA）实现的数字签名算法。

虽然其性能优于现有的 ECDSA 实现，不过，它并不会完全取代 JDK 中现有的椭圆曲线数字签名算法( ECDSA)。

```java
KeyPairGenerator kpg = KeyPairGenerator.getInstance("Ed25519");
KeyPair kp = kpg.generateKeyPair();
 
byte[] msg = "test_string".getBytes(StandardCharsets.UTF_8);
 
Signature sig = Signature.getInstance("Ed25519");
sig.initSign(kp.getPrivate());
sig.update(msg);
byte[] s = sig.sign();
 
String encodedString = Base64.getEncoder().encodeToString(s);
//0Hc0lxxASZNvS52WsvnncJOH/mlFhnA8Tc6D/k5DtAX5BSsNVjtPF4R4+yMWXVjrvB2mxVXmChIbki6goFBgAg==
System.out.println(encodedString);
```

### **密封类（Sealed Classes）**

在该版本以前，如果要限制某个类不允许被继承和修改就必须使用final对其修饰，可以看到并不灵活。在该版本中就通过关键字的加入来限制某些类可以继承，没有标注的则没办法继承。

密封类可以对继承或者实现它们的类进行限制，这样这个类就只能被指定的类继承。另外，任何扩展密封类（子类）的类本身都必须声明为 `sealed`、`non-sealed` 或 `final`。

- sealed：类和接口上可以添加sealed关键字，用来限定自己可以派生出哪些子类.换句话说，就是控制哪些子类可以继承或者实现自己，不允许其他类派生。

```java
//Person只允许User 和 Bill继承。
public sealed class BaseEntity implements Serializable permits User, Bill
 
public non-sealed/sealed/final class User extends BaseEntity implements Serializabl
public non-sealed/sealed/final class Bill extends BaseEntity implements Serializabl
```

- non-sealed：non-sealed关键字表示允许未知的子类继承被修饰的类。如下述代码所示，Sub2在没有permits关键字的情况下也可以被Any1和Any2继承;

```java
public sealed class Super permits Sub1, Sub2{
 
}
final class Sub1 extends Super{}
non-sealed class Sub2 extends Super{}
class Any1 extends Sub2{}
class Any2 extends Sub2{}
```

与record关键字配合使用，record关键字是隐式的final，所以record也可以不加final继承密封类：

```java
public sealed interface Employee permits CLevel, Programmer{
 
}
record CLevel(String type) implements Employee{}
record Programmer(String experience) implements Employee{}
```

getPermittedSubclasses方法，通过反射的getPermittedSubclasses方法我们可以直接获取被允许继承的子类,也就是permits关键字允许的类。

```java
public sealed class Color permits Red,Green,Blue{
 
}
final class Red extends Color{}
final class Green extends Color{}
final class Blue extends Color{}
public class PermittedSubCLasses {
    public static void main(String[] args) {
        for(var p: Color.class.getPermittedSubclasses()) {
            System.out.println(p.getSimpleName());
        }
    }
}
```

---

密封类的作用：

> 让类的作者可以精确的控制子类，保持不被滥用扩展了原有的权限修饰符，限制父类的使用不再一刀切的开放或者私有为未来的模式匹配打好基础，指定有限子类之后使模式匹配的分析更简单——就是为了switch模式匹配开发的。
> 

## JDK16

### **摘要**

这个版本基本没啥大改变，基本就是以前孵化的功能现在变为正式功能，所以下面就说一下新的

### **记录类型**

从JDK16开始，非静态内部类可以定义非常量的静态成员。

```java
public class Outer {
    class Inner {
        static int age;
    }
}
```

### **其他**

剩下的感觉没有什么特别重要的用法，而是一些细枝末节的修改优化

[JDK16概述（1）](https://javaguide.cn/java/new-features/java16.html#%E5%85%B6%E4%BB%96%E4%BC%98%E5%8C%96%E4%B8%8E%E6%94%B9%E8%BF%9B)

[JDK16概述（2）](https://cloud.tencent.com/developer/article/1900076)

# **JDK17（LTS）**

### **背景**

- 公司开始将逐渐拥抱JDK17
- 后续的Spring全家桶的鞭打：Spring 6.x 和 Spring Boot 3.x 最低支持的就是 Java 17
- 多个JDK版本孵化特性的一个阶段性开花结果——ZGC、密封类、Switch等等

### **前言**

这里只会介绍新特性，对于那些孵化完成在本版本正式退出的特性不再做过多赘述，读者可以自行翻阅以前的文章

### **增强的伪随机数生成器**

[PRNG的介绍](https://ctf-wiki.org/crypto/streamcipher/prng/intro/)，在代码中PRNG依赖于一个初始值，即为种子，在下面代码中就使用了当前的时间戳来作为种子。[详细评测](https://www.v8en.com/article/286)

> 通过RandomGeneratorFactory.of所创建的随机工程，需要的这个字符串代表的是随机数生成器
> 
> 
> ![image.png](JDK%E7%AE%80%E8%A6%81%E8%BF%9B%E5%8C%96%E5%8F%B2%EF%BC%8811~17%EF%BC%89/image%201.png)
> 

```java

@Test
public void testRandomSeed(){
    RandomGeneratorFactory<RandomGenerator> random = RandomGeneratorFactory.of("Xoroshiro128PlusPlus");
    RandomGenerator randomGenerator = random.create(DateKit.now().getTime());
    System.out.println(randomGenerator.nextInt(100));
}
```

---

### **Switch的类型匹配**

前面介绍JDK12中介绍过instanceof的类型匹配，可以省一些代码的编写，现在switch也可以在参数中进行类型匹配

```java
/*-----------------旧-------------------------*/
static String formatter(Object o) {
    String formatted = "unknown";
    if (o instanceof Integer i) {
        formatted = String.format("int %d", i);
    } else if (o instanceof Long l) {
        formatted = String.format("long %d", l);
    } else if (o instanceof Double d) {
        formatted = String.format("double %f", d);
    } else if (o instanceof String s) {
        formatted = String.format("String %s", s);
    }else if (o == null){
        return "oops!";
    }
    return formatted;
}
 
/*-----------------新-------------------------*/
static String formatterPatternSwitch(Object o) {
    return switch (o) {
        case null      -> return "oops!";
        case Integer i -> String.format("int %d", i);
        case Long l    -> String.format("long %d", l);
        case Double d  -> String.format("double %f", d);
        case String s  -> String.format("String %s", s);
        default        -> o.toString();
    };
}
```

---

### **其他**

[JDK17其他新特性](https://javaguide.cn/java/new-features/java17.html)