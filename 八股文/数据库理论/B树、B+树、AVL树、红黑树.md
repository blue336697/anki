# B树、B+树、AVL 树、红黑树

- B + 树，数据库懂吗，这是 innoDB 的后端实现，给我讲一下 B 树和 B + 树

> 讲 b + 树先要讲 b 树，思想和二叉搜索树类似，二叉树二路搜索，i/o 较高，b 树又叫 m 路多路平衡搜索树…，是为了多级储存设计 balabala，讲了一下搜索、插入删除，上下溢，节点既存 key 也存 value，调整消耗大，不适合顺序遍历。b + 树解决顺序搜索问题，数据链式维护在硬盘，仅把 key 提出来做索引，类似 B 树，key 定位到有指针指向物理位置，遍历即可。特点 1. 非叶节点仅存 key，小，好维护，2. 每个节点大小设置为扇区大小，查找下一节点顺序遍历，不用寻道了，快。另外数据库一点不会不会。
> 
- 那节点设置成多大好呢？

> 一般设置成扇区大小啊，扇区硬盘厂商设定的。
> 
- 那我换个问法，现在一块 HDD7200 转，寻道时间 2ms，寻址时间 4ms，问块大小设置成多少，最大 qps 最大能到多少？

> 每次读取数据花费的时间可以分为寻道时间、旋转延迟、传输时间三个部分，寻道时间指的是磁臂移动到指定磁道所需要的时间，主流磁盘一般在5ms以下；旋转延迟就是我们经常听说的磁盘转速，比如一个磁盘7200转，表示每分钟能转7200次，也就是说1秒钟能转120次，旋转延迟就是1/120/2 = 4.17ms；传输时间指的是从磁盘读出或将数据写入磁盘的时间，一般在零点几毫秒，相对于前两个时间可以忽略不计。那么访问一次磁盘的时间，即一次磁盘IO的时间约等于5+4.17 = 9ms左右
> 

我：蒙了。算，嗯？寻址时间给我来我还算啥？没搞懂。iops = 寻道 + 旋转 + 寻址 = 2+60*1000/7200/2（平均转半个盘定位）+4。qps=1000/iops