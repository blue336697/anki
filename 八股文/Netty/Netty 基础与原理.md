# Netty | 基础与原理

type: Post
status: Published
date: 2023/04/20
summary: 基础与原理
tags: Netty
category: 中间件

# 一、Netty基础篇

## 1.搭建Netty服务器端

![image.png](Netty%20%E5%9F%BA%E7%A1%80%E4%B8%8E%E5%8E%9F%E7%90%86/image.png)

![image.png](Netty%20%E5%9F%BA%E7%A1%80%E4%B8%8E%E5%8E%9F%E7%90%86/image%201.png)

## 2.搭建Netty客户端

![image.png](Netty%20%E5%9F%BA%E7%A1%80%E4%B8%8E%E5%8E%9F%E7%90%86/image%202.png)

![image.png](Netty%20%E5%9F%BA%E7%A1%80%E4%B8%8E%E5%8E%9F%E7%90%86/image%203.png)

![image.png](Netty%20%E5%9F%BA%E7%A1%80%E4%B8%8E%E5%8E%9F%E7%90%86/image%204.png)

- 服务端和客户端总的一个流程图

![image.png](Netty%20%E5%9F%BA%E7%A1%80%E4%B8%8E%E5%8E%9F%E7%90%86/image%205.png)

## 3.短连接总结

- 缺点

> 通过以上的介绍，对Netty客户端会有一个基本的了解，能正常发 送与接收数据。上述用法看起来没什么问题，但性能偏弱。因为在每 次发送请求时都需要创建连接，还不如直接使用普通Socket作为客户 端。可以运用连接池优化解决，把短连接先放入连接池，然后从连接 池中获取每次请求的连接
> 
- 优化方向

> 即从短连接优化成长连接
> 

## 4.优化

### 4.1 优化前需要思考的两个问题

- 概述

> 一般在公司中RPC通信调用服务都是长连接，那么请思考两个问题
> 
1. 改为长连接后，客户端的`handler`不能每次在`main`方法中构建，`promise`对象无法通过主线程传送给客户端的`handler`，那么此时客户端主线程如何获取`NioEventLoop`线程的数据呢？

![image.png](Netty%20%E5%9F%BA%E7%A1%80%E4%B8%8E%E5%8E%9F%E7%90%86/image%206.png)

1. 客户端主线程每次获取的响应结果对应那次请求呢？
- 解决方式

> 对于第一个问题，通过多线程交互可以解决，首先对`Netty`客户端创建的连接进行静态化处 理，以免每次调用时都需要重复创建。然后在给服务端发送请求后运 用`RequestFuture的get()`方法同步等待获取响应结果，以替代`Netty的Promise` 同 步 等 待 ； 最 后 用 `RequestFuture.received` 替代`ClientHandler的Promise`异步通知
> 

> 对于第二个问题就是利用每次发送请求带上自增的唯一id，客户端缓存起来，服务器端接收后会把id放在响应结果中一起返回
> 

# 二、原理部分

## 1.多路复用器

- 概述

> NIO有一个非常重要的组件——**多路复用器**，其底层有3种经典模 型，分别是`epoll、select和poll`。与传统`I/O`相比，一个多路复用器 可以处理**多个**`Socket`连接，而传统`I/O对每个Socket`连接都需要一条线 程去同步阻塞处理。NIO有了多路复用器后，**只需一条线程即可管理多 个Socket连接的接入和读写事件。Netty的多路复用器默认调用的模型 是epoll模型。它除了JDK自带的epoll模型的封装，还额外封装了一 套，它们都是epoll模型的封装，只是JDK的epoll模型是水平触发的，而Netty采用JNI（本地方法）重写的是边缘触发。**
> 

> • 边缘触发 是指每当状态变化时发生一个io事件；
• 水平触发 是只要满足条件就发生一个io事件；
> 

### 1.1 NIO和BIO

- 阻塞式IO

> 例如，客户端向服务器发送10B的数据，若服务器一次 只接收到8B的数据，则必须一直等待后面2B大小的数据的到来，在后面2B的数据未到达时，当前线程会阻塞在接收函数处。
> 
- BIO服务流程图

> 同步阻塞IO是最简单的IO模型，用户线程在内核进行IO操作时被阻塞。用户线程通过调用系统调用read发起IO读操作，由用户空间转到内核空间。内核等到数据包到达后，然后将接受的数据拷贝到用户空间，完成read操作。整个IO请求过程，用户线程都是被阻塞的，对CPU利用率不够
> 

![image.png](Netty%20%E5%9F%BA%E7%A1%80%E4%B8%8E%E5%8E%9F%E7%90%86/image%207.png)

- 非阻塞式IO

> **从TCP缓冲区中读取数据，缓冲区中的数据可分多次 读取，不会阻塞线程。例如，已知后面将有10B的数据发送过来，但是 如果现在缓冲区只收到8B的数据，那么当前线程就会读取这8B的数 据，读完后立即返回，等另外2B的数据发来时再去读取。NIO中的多路 复用器管理成千上万条的Socket连接。**
> 

> **所以若还是用阻塞的方式当多路复用器每次从TCP缓冲区 读数据时，若有些客户端数据包未能全部到达，且读取数据的线程是 在阻塞的情况下，则只有全部数据到达时才能返回，这样不仅性能弱，还不可控，无法预测等待的时间。**
> 
- NIO

> 如果只是单纯的NIO框架，在同步基础上，将socket设置为NONBLOCK，这样用户线程可以在发起IO请求后立即返回。虽说可以立即返回，但并未读到任何数据，用户线程需要不断的发起IO请求，直到数据到达后才能真正读到数据，然后去处理。 整个IO请求中，虽然可以立即返回，但是因为是同步的，为了等到数据，需要不断的轮询、重复请求，消耗了大量的CPU资源。因此，这种模型很少使用，实际用处不大。
> 
- NIO+多路复用器

> 可以看到比BIO多了个selector，但结构要复杂很多，一个selector能监听多个channel，运行select方法会循环询问是否有就绪状态的channel，可以看到与BIO相比，并发能力大大提高
> 

![image.png](Netty%20%E5%9F%BA%E7%A1%80%E4%B8%8E%E5%8E%9F%E7%90%86/image%208.png)

### 1.2 epoll模型和select模型的区别

- 概述

> IO多路复用器单个进程能处理多个描述符的IO，Java通过调用复用器来获得多个描述符，已进行读写操作，底层实现最经典的就是epoll和select模型
> 

### 1.2.1 select模型的特点

1. `select`模型只有一个`select`函数，每次在调用`select`函数 时，都需要把整个文件描述符集合从用户态拷贝到内核态，当文件描述符很多时，开销会比较大
2. 每次在调用`select`函数时，内核都需要遍历所有的文件描述符，这个开销也很大，尤其是当很多文件描述符根本就无状态改变时，也需要遍历，浪费性能
3. `select`可支持的文件描述符有上限，可监控的文件描述符个 数取决于`sizeOf(fd_set)`的值。如果`sizeOf(fd_set)=512`，那么此服务器最多支持`512×8=4096`个文件描述符。

### 1.2.2 epoll模型

- 概述

> 先来介绍`epoll`的三个函数
> 
1. `int epoll_create(int size)：用于创建一个epoll句柄`
2. `int epoll_ctl(int epfd, int op, int fd, structepoll_event*event)`

> • 第一个参数：epoll_create函数调用返 回的值
• 第二个参数：表示操作动作，由三个宏（`EPOLL_CTL_ADD`表示注 册新的文件描述符到此`epfd`上，`EPOLL_CTL_MOD`表示修改已经注册的文 件描述符的监听事件，`EPOLL_CTL_DEL`表示从`epfd`中删除一个文件描述 符）来表示
• 第三个参数：为需要监听的文件描述符；
• 第四个参数：表示 要监听的事件类型，事件类型也是几个宏的集合，主要是文件描述符 可读、可写、发生错误、被挂断和触发模式设置等
> 
1. `epoll_wait：表示等待文件描述符就绪。`
- 相较于select改善的地方

> • 所有需要监听的文件描述符只需在调用第二个函数拷贝一次即可，如发生改变，则放入一个就绪队列通过epoll_wait函数获取文件的描述符
• 调用第三个函数只会遍历发生状态变化的文件描述符
• 没有文件描述符的个数限制
• 采用了内存映射机制，内核直接将就绪队列映射到用户态，避免了内存拷贝带来的性能开销
> 
- epoll的两种工作模式

> epoll有EPOLLLT和EPOLLET两种触发模式，LT是默认的模式，ET是“高速”模式。LT模式下，只要这个fd还有数据可读，每次 epoll_wait都会返回它的事件，提醒用户程序去操作，而在ET（边缘触发）模式中，它只会提示一次，直到下次再有数据流入之前都不会再提示了，无 论fd中是否还有数据可读。
> 

> 所以在ET模式下，read一个fd的时候一定要把它的buffer读光，也就是说一直读到read的返回值小于请求值，或者 遇到EAGAIN错误。还有一个特点是，epoll使用“事件”的就绪通知方式，通过epoll_ctl注册fd，一旦该fd就绪，内核就会采用类似callback的回调机制来激活该fd，epoll_wait便可以收到通知。
> 

> 当没有 IO 事件的时候， epoll 也是会阻塞掉当前进程。这个是合理的，因为没有事情可做了占着 CPU 也没啥意义。epoll 本身是阻塞的，但一般会把 socket 设置成非阻塞。
> 
- 这里Epoll是有一个空轮询bug的下面有讲

### 1.2.3 poll模型

- 概述

> 这里再额外插一嘴，poll模型，这个模型本质上跟select没有区别，只不过跟epoll一样也不限制文件描述符的数量，因为通过链表实现
> 

### 1.2.4 select和poll模型的缺点

> **从上面看，select和poll都需要在返回后，通过遍历文件描述符来获取已经就绪的socket。事实上，同时连接的大量客户端在一时刻可能只有很少的处于就绪状态，因此随着监视的描述符数量的增长，其效率也会线性下降。**
> 

## 2.Netty线程模型

- 概述

> **Netty拥有两个线程组，为Boss线程组和Worker线程组，Boss线程组一般为一条线程，Worker默认为CPU核心数的两倍，Boss监听socket的变化和op_accept事件和客户端的连接；当Boss听到有socket接入，会转换为nioSocket，然后注册到worker中的选择器中，让worker监听到某个socket就绪状态就会进行下面流程**
> 
- 流程

> 内存池分配内存，读IO解码，若能成功解码交给handler处理经过处理进行编码写到缓冲区，由IOworker线程输出到网络并传输给客户端
> 
- 主从线程模型

![image.png](Netty%20%E5%9F%BA%E7%A1%80%E4%B8%8E%E5%8E%9F%E7%90%86/image%209.png)

### 2.1 Netty基于Reactor模式

- 什么是Reactor模式

> 其实Reactor模式就是使用一个IO连接的复用器，用这个复用器去分发任务
Reactor模式的分类
> 
- 单线程Reactor

![image.png](Netty%20%E5%9F%BA%E7%A1%80%E4%B8%8E%E5%8E%9F%E7%90%86/image%2010.png)

- 多线程Reactor

![image.png](Netty%20%E5%9F%BA%E7%A1%80%E4%B8%8E%E5%8E%9F%E7%90%86/image%2011.png)

- 主从多线程Reactor

![image.png](Netty%20%E5%9F%BA%E7%A1%80%E4%B8%8E%E5%8E%9F%E7%90%86/image%2012.png)

## 3. 编码、解码

- 概述

> 对于使用原生Java NIO来完成TCP传输，那么要考虑很多如拆包、粘包导致的半包问题和序列化问题
> 
- TCP通信图

![image.png](Netty%20%E5%9F%BA%E7%A1%80%E4%B8%8E%E5%8E%9F%E7%90%86/image%2013.png)

- 粘包问题的产生

> 在两个TCP输出缓冲区节点中，由于TCP会实现流量控制和拥塞控制，对于太多的数据包会进行分段传输，太少的数据包会等待足够多时在传输，很显然就是产生粘包问题的因素
> 
- 服务端进行拆包

> 当服务端读取TCP输入缓冲区的数据时，需要进行拆包处理并解决粘包和拆包，一般有三种方法在应用层：
> 
> 1. 将换行符号或特殊标识符号加入数据包中，如HTTP和FTP等。（HTTP 协议通过设置回车符、换行符作为 HTTP header 的边界，通过 Content-Length 字段作为 HTTP body 的边界，这两个方式都是为了解决“粘包”的问题）
> 2. 将消息分为head和body，head中包含body长度的字段，一般前 面4个字节是body的长度值，用int类型表示，但也有像Dubbo协议那种，head中除body长度外，还有版本号、请求类型、请求id等。
> 3. 固定数据包的长度，如固定100个字节，不足补空格。
- Netty如何解决粘包

> 对于Netty来说，对上面的三种方法都进行了具体实现，采用模板方法设计模式实现了一套编码解码的架构：
> 
> 1. 第一种`Netty`有解码器`LineBasedFrameDecoder`，可以判断字节中是否出现了`“\\n”或“\\r\\n”`。
> 2. `Netty` 有 编 解 码 器 `LengthFieldPrepender 和LengthFieldBasedFrameDecoder`，可以在消息中加上消息体长度值，这两个编解码器在之前的实战中用到过。
> 3. `Netty` 有 固 定 数 据 包 长 度 的 解 码 器`FixedLengthFrameDecoder`。此方案一般用得较少，比较常用的是前两种方案。

## 4.序列化

- 概述

> 由于在网络传输中，都是以二进制数据流来传输，不能直接传输java对象，所以必须对Java对象内容进行流化下面介绍集中常见的
> 
- Java自带序列化

> 费用非常简单，但基本不用，流化后的数据还是很大，性能太低，并且无法进行跨语言的序列化
> 
- 其他序列化方式

> 例如Protobuf、Kryo、JSON等，现在web对JSON的支持很好，并且市面上将Java转换成JSON的工具类也很多，比如FastJson、Jackson；
> 
- 序列化ID

> **携带序列化ID主要为了序列化和反序列化做准备，我们就知道序列化是根据哪个类，反序列化就能通过序列号找到这个类进行反序列化**
> 

### 4.1 Protobuf序列化

- 概述

> Protobuf是Google提供的一个具有高效协议数据交换格式的工具 库（类似JSON），但Protobuf有更高的转化效率，且时间效率和空间 效率都是JSON的3～5倍
> 

![image.png](Netty%20%E5%9F%BA%E7%A1%80%E4%B8%8E%E5%8E%9F%E7%90%86/image%2014.png)

- 优点

> Protobuf序列化除了占用空间小，性能还非常好，主要是它带有 属性值长度，无须进行字符串匹配，这个长度值只占用1B的存储空 间。另外，JSON都是字符串解析，而Protobuf根据不同的数据类型有不同的大小，如bool类型只需读取1B的数据。
> 
- 缺点

> • 无法正确的对复杂的Java对象进行反序列化
• 需编写.proto文件较麻烦
• 可读性差
> 

### 4.2 Kryo序列化

- 概述

> Kryo，它是一个快速高效的Java对象图形序列化框架，主要特点是性能高、高效和易用。它的存 储与Protobuf的存储一样，采用了可变长存储机制
> 
- 实战注意

![image.png](Netty%20%E5%9F%BA%E7%A1%80%E4%B8%8E%E5%8E%9F%E7%90%86/image%2015.png)

![image.png](Netty%20%E5%9F%BA%E7%A1%80%E4%B8%8E%E5%8E%9F%E7%90%86/image%2016.png)

### 4.3 几种的对比

> JSON序列化后的数据体积较大，不适合网络传输 和海量数据存储。Protobuf和Kryo序列化后的体积与JSON相比要小很 多，但是JSON受众面大
> 

## 5.零拷贝

- 概述

> 序列化主要与传输数据格式有关，不管是Kryo还是Protobuf，它 们都能对数据内容进行压缩，并能完整地恢复。零拷贝是Netty的一个 特性，主要发生在操作数据上，无须将数据Buffer从一个内存区域拷 贝到另一个内存区域，少一次拷贝，CPU效率就会提升。
> 
- 应用场景

> 
> 
> 1. 直接使用堆外内存对Socket进行读写操作，传统是需要将堆外内存（也就是系统的缓存）读取到JVM的堆内内存在进行处理（AbstractReferenceCountedByteBuf ）
> 2. 不对TCP传输的数据包进行拷贝而是进行直接的逻辑组合，类似于数据库的视图（CompositeByteBuf）
> 3. 传统在进行拷贝文件时，会将文件输入流放到临时的数组，在通过输出流，将临时的数组写入到目标文件中，而Netty使用NIO的channel的transfer可以直接将文件缓冲区的数据发送到目标Channel，避免了传统的通过循环写方式导致的内存数据拷贝问题。

## 6.背压

- TCP链路背压场景

> **当消费者的消费速率低于生产者的发送速率 时，会造成背压**，此时消费者无法从TCP缓存区中读取数据，因为它无 法再从内存池中获取内存，从而造成TCP通道阻塞。生产者无法把数据 发送出去，这就使生产者不再向缓存队列中写入数据，从而降低了生 产速率。当消费者的消费速率提升且TCP通道不再阻塞时，生产者的发送速率又会得到提升，整个链路运行恢复正常。
> 

![image.png](Netty%20%E5%9F%BA%E7%A1%80%E4%B8%8E%E5%8E%9F%E7%90%86/image%2017.png)

### 6.1 TCP窗口

- 概述

> Netty的背压底层是依靠TCP的流量控制来完成的，说到流量控制就不得不提流量控制的关键TCP窗口，分为发送缓冲区和发送窗口和接收缓冲区和接收窗口。因为窗口可改变，所以称为滑动窗口。
> 
- 窗口的重要参数

> • 包的序号，用来解决包的乱序问题（虽然SR选择性重发协议在接收端可以接收乱序的，但是交给上层时还是需要按照序号顺序交）
• ACK（确认序号），用来解决丢包问题
• TCP窗口，用来解决流控
• TCP FLAG，包的类型，主要操控TCP的11中状态机
> 
- 流程图（图中窗口为接收端的窗口大小哦）

> **主机A与主机B通信，在第一次发送数据时，主机A发 送多少数据到网络中由链路带宽的大小来决定。同时，主机B在收到数据段后，会把下一个要接收的数据序号返回给主机A，即图中的ACK。**
> 

![image.png](Netty%20%E5%9F%BA%E7%A1%80%E4%B8%8E%E5%8E%9F%E7%90%86/image%2018.png)

### 6.2 这里简单提一嘴Flink

- 概述

> 一些分布式计算引擎主要是利用了TCP窗口的特性，因此，无须加太多额外处理流程就能完成整套架构的背压，如Flink。
> 

![image.png](Netty%20%E5%9F%BA%E7%A1%80%E4%B8%8E%E5%8E%9F%E7%90%86/image%2019.png)

- 总结

> **你就记住Flink的背压原理跟Netty差不读，都是除了系统的TCP缓冲区又引入了一个本地缓冲池，目的是为每个Channel预先分配内存，减少内存在分配过程中的多线程竞争，提高性能。**
>